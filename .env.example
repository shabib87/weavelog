# weavelog live-root configuration (copy to .env at the live root; never commit .env)
# WEAVELOG_HOME: where weavelog materializes its live root (default ~/.agents)
WEAVELOG_HOME=$HOME/.agents
# WEAVELOG_CONFIG_HOME: opencode config dir (default ~/.config/opencode)
WEAVELOG_CONFIG_HOME=$HOME/.config/opencode