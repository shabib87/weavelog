---
id: TASK-45
title: >-
  Port ~/.agents to flightlead v0.1.0 — execution plan + checklist (refined from
  decision map)
status: In Progress
assignee:
  - '@conductor'
created_date: '2026-09-05 06:01'
updated_date: '2026-09-06 06:09'
labels:
  - 'wayfinder:map'
  - spec-approved
dependencies: []
references:
  - docs/research/2026-09-02-oss-agent-harnesses.md
  - docs/research/2026-09-03-loopeng-packaged-distribution.md
  - docs/research/2026-09-02-loopeng-moat-analysis.md
  - docs/specs/2026-09-05-v010-draft-brief.md
ordinal: 35000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The single execution task for porting the personal harness (~/.agents) into its OSS repo (~/Projects/flightlead) for v0.1.0. All ratified decisions live in docs/specs/2026-09-05-v010-draft-brief.md (the brief, in ~/Projects/flightlead). This task carries the ordered, verifiable port checklist (PORT PLAN v8, reviewer-amended [AMEND-R5-20]) and EARS acceptance criteria.

Plan review: v8 incorporates every finding from the failed review rounds (v2 and v4) plus the round-5 amendment pass [AMEND-R5-1..21; R5-22 verified no-op]: CLI build, runbook decomposition, diagram-design symlink disposition, privacy-audit artifact, test-runner alignment, Step 3 pre-destruct checkpoint with LaunchAgent-first, hygiene step 0.6, publish gating, AC corrections, enumerated 0.1 must-fixes, enabler dispositions — plus the round-3 amendments: AC#5 dual-anchored rewrite (POST-FLIP ONLY-LIST + untouched $HOME dirs), copy-based backlog/ switchover, 3.2-pre rewrite, plist bootout-only safety + log-path repoint, single host-skills path unification, skip-when-absent CI timing, 1.11b files/build-output fix, pre-Step-1 backup manifest, and AC#4 byte-identity clarification. GATE 0: unanimous 4-reviewer PASS is required before Step 1 executes; verdicts recorded in Implementation Notes.

Execution happens on a NEW thread under the conductor pattern; this task is the single source of truth. A verified backup of ~/.agents exists (human-made, 2026-09-05) — rollback = restore backup per the AC#11 recovery protocol.

PERSONAL (excluded from port): in-my-voice skill (stays at ~/.agents/skills/in-my-voice), tldraw-offline skill (lives at ~/.claude/skills/tldraw-offline, untouched), tldraw code dependency, diagram-design content (policy symlink only, see 1.2).

Settled decisions referenced (do not re-open): backlog.md is CLI-only, no MCP (recorded backlog-lifecycle.md:72, 2026-08-29); Apache-2.0 license + NOTICE; arm64 macOS only; npm at 0.1.0, brew at 0.2+; ATTRIBUTION = code actually taken only + Addy Osmani conceptual shoutout.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 #1 #1 WHEN the port completes THEN all payload assets SHALL exist under ~/Projects/flightlead in their ratified disposition locations per this INLINE asset->location mapping [AMEND-R4-11] (no deferral to the brief): config/ -> payload/config/; skills/ minus personal -> payload/skills/; bin/src + bin/test -> src/ + tests/ (all 15 suites mapped per 1.3); plugins/enforce.ts + verify-gate.ts -> src/hooks/; stack-versions.json -> flightlead.json manifest seed; AGENT-STACK-RUNBOOK.md -> decomposed per 1.10; AGENTS.md -> payload/AGENTS.md (path-templated payload per 1.9 [AMEND-R5-5]); docs/architecture/ -> docs/architecture/; research notes -> docs/research/ (EXACTLY the two public-safe notes per 1.5); backlog/ -> flightlead/backlog/ (COPY per 1.6; the ~/.agents source is deleted in 3.3 [AMEND-R5-21]); and the PERSONAL EXCLUSIONS (never ported): in-my-voice skill (stays at ~/.agents/skills/in-my-voice), tldraw-offline skill (lives at ~/.claude/skills/tldraw-offline — untouched), tldraw code dependency, and diagram-design content (init-time policy symlink only, per 1.2)
- [ ] #2 #2 #2 IF scripts/privacy-audit (the checked-in artifact holding the exact grep command, the personal-strings list, and declared exclusion paths for NOTICE copyright lines, the README trademark line, and the backlog/ copy per 1.12 [AMEND-F6]) runs against ~/Projects/flightlead THEN it SHALL exit 0 with zero hits outside those exclusions, and WHEN payload/config/agents/researcher.md and implementer.md are inspected THEN they SHALL contain templated paths with zero hardcoded /Users/shabibhossain paths in payload/config
- [ ] #3 #3 #3 WHEN `npm test` runs in ~/Projects/flightlead (`node --import tsx --test "tests/**/*.test.ts"`) THEN every suite ported under tests/**/*.test.ts SHALL pass (Step 1.3 enumerates ALL 15 bin/test suites and maps each into tests/, or records a per-suite disposition) and grep SHALL find zero remaining bun:test imports under tests/
- [ ] #4 #4 #4 WHEN `flightlead sync` runs against a scratch live-root THEN it SHALL exit 0, and WHEN it runs against a NON-TEMPLATED PAYLOAD file [AMEND-R4-2 — the byte-identity example is payload/config/opencode.jsonc ONLY; payload/AGENTS.md is a PATH-TEMPLATED payload file per 1.9 [AMEND-R5-5] and is no longer cited as the example; config-sync.ts is NOT an example: it ports to src/ per the port matrix and sync never materializes it] THEN the materialized file SHALL be byte-identical to the payload template (zero .env injection occurs at template-resolve time for non-templated files; byte-identity asserted on the non-templated payload file), and WHEN it runs against TEMPLATED files THEN the test SHALL assert the RESOLVED values (template variables substituted from .env) rather than raw-template byte-identity
- [ ] #5 #5 #5 WHEN the flip completes THEN (a) POST-FLIP ONLY-LIST: ~/.agents SHALL contain ONLY this exact enumerated list, each survivor with an explicit fate: AGENTS.md (keep); .gitignore (keep); .env (keep, unversioned, per Step 0.7 — post-flip sync writes .env from 0.7's secrets mechanism [AMEND-R5-4]); LICENSE (keep, relicensed IN PLACE MIT -> Apache-2.0 per Step 0.1 item (9) live-root relicensing [AMEND-R4-5]); docs/AUTHORING.md (keep); docs/plans/ (keep, 8 personal plan files); docs/spec/ (keep, personal design notes); docs/research/ (keep the non-ported personal notes; the two ported public-safe note COPIES are deleted here per 3.3); skills/ (host skills dir per the 1.2 single-path decision: personal in-my-voice stays + the init-time diagram-design symlink per the init policy) [AMEND-F1: .pi/ and .headroom/ REMOVED from this list — they are $HOME-level dirs, verified absent from ~/.agents, and belong ONLY in (b)] — and NOTHING else: no .git, no .worktrees, no node_modules, no state, no logs, no reports/, no backups, no backlog/ (the 1.6 copy-left source is deleted per 3.3), no config/, bin/, plugins/, stack-versions.json, AGENT-STACK-RUNBOOK.md, docs/architecture/; CLAUDE.md and docs/tbd/ DO NOT EXIST under ~/.agents (verified 2026-09-05) — if present, delete; and `test ! -d ~/.agents/.git` SHALL pass; and (b) $HOME DIRS LEFT UNTOUCHED (outside ~/.agents): ~/.pi and ~/.headroom (verified on disk 2026-09-05), ~/.pi-subagents (brief matrix row; verified ABSENT on disk 2026-09-05 — N/A, nothing to preserve [AMEND-R4-13]), ~/.claude/ including ~/.claude/skills/tldraw-offline (verified to live there, NOT in ~/.agents/skills), and ~/Library/LaunchAgents/com.headroom.proxy.plist SHALL not be modified by the flip
- [ ] #6 #6 #6 WHEN the built CLI is invoked THEN each of init, sync, update, check, doctor, and `scaffold --project` SHALL run against a real install and exit 0 on a healthy setup — DEFINITIONS [AMEND-R5-19]: "a real install" = `npm pack && npm i -g ./flightlead-*.tgz` on a clean arm64 macOS fixture; "healthy" = `flightlead doctor` exiting 0 on that same fixture — and WHEN `flightlead init` runs on a clean arm64 macOS machine THEN it SHALL produce a loadable opencode config, skills on a scanned path, >=1 enforced gate loaded in opencode, a written flightlead.json manifest, an appending JSONL ledger at ~/.local/state/flightlead/, and a fully green `flightlead doctor`; and package.json `bin` SHALL point at the built dist/ JS output (never raw TS requiring tsx at runtime), per Step 1.11b
- [ ] #7 #7 #7 WHEN the Step 0.1 must-fixes are applied THEN package.json license SHALL read Apache-2.0, ATTRIBUTION.md SHALL list only code-actually-taken with links + licenses plus the Addy Osmani conceptual shoutout, the semgrep invoked-not-bundled redistribution clause SHALL be present, and zero REVIEWER placeholder comments SHALL remain in any tracked file
- [ ] #8 #8 #8 WHEN the flip completes THEN daily conductor work SHALL run from ~/Projects/flightlead, verified by this CONCRETE COMMAND BATTERY [AMEND-R5-18] (each named command from ~/Projects/flightlead with its expected exit code): (1) `backlog task list` -> exit 0; (2) `flightlead doctor` -> exit 0; (3) `flightlead check` -> exit 0; (4) `git status --porcelain` -> exit 0 — and ~/.zshrc SHALL be unmodified, verified as: Step 0.9b (numbered pre-Step-1 action) records the baseline `shasum -a 256 ~/.zshrc > ~/backups/zshrc.sha256.pre-step-1.txt` and the verification re-hash MUST match that recorded sha256; com.headroom.proxy.plist SHALL be untouched (it targets ~/.local/bin/headroom via pipx — NO rewrite needed) with :8788 healthy across the flip via a concrete doctor check; com.agents.stack-check.plist SHALL be handled per the recorded Step 3.1 disposition [AMEND-F4] (MIGRATE to the built flightlead check via the NEW label/path com.flightlead.check.plist; bootout only, old com.agents.stack-check.plist kept BYTE-UNTOUCHED until deleted in 3.3; log paths repointed to ~/.local/state/flightlead/)
- [ ] #9 #9 #9 WHEN AGENT-STACK-RUNBOOK.md decomposition completes THEN "section" SHALL mean one H2 heading of AGENT-STACK-RUNBOOK.md [AMEND-R4-10], and a committed HEADING INVENTORY (the exhaustive checklist of every H2 heading PLUS the pre-first-H2 preamble block — first H2 verified at line 24; inventory = every H2 + the preamble [AMEND-R5-12], checked in as an artifact) SHALL assign every section exactly one fate (script / docs/architecture/<topic>.md / payload template / delete) with zero sections remaining prose-only
- [ ] #10 #10 #10 WHEN payload/skills is inspected THEN `find payload/skills -type l` SHALL return ZERO symlinks (diagram-design content excluded; the diagram-design link is an init-time host artifact created at the SINGLE host skills path ~/.agents/skills/diagram-design with real target ../../code/diagram-design/skills/diagram-design), and WHEN `flightlead init` runs THEN the init policy SHALL create that host symlink only when the target exists (else skip + doctor warning), and tests SHALL assert the init policy, not payload symlink contents; AC#5, AC#10, Step 1.2, and Step 3.3 all name ~/.agents/skills/ consistently, and personal in-my-voice REMAINS at ~/.agents/skills/in-my-voice post-flip
- [ ] #11 #11 #11 IF any step fails verification THEN the operator SHALL follow the recovery protocol: return to the most recent enumerated pre-destruct checkpoint (post-0.6, post-1.13 verify, or Step 3.2), restore ~/.agents from ~/.agents-backup-sept-5-2026.zip (zip root contains .agents/ only; manifest recorded at ~/backups/agents-backup-sept-5-2026.manifest.txt, generated pre-Step-1 per Step 0.9 via `unzip -l ~/.agents-backup-sept-5-2026.zip > ~/backups/agents-backup-sept-5-2026.manifest.txt`) using the exact restore command `rm -rf ~/restore-staging && ditto -x -k ~/.agents-backup-sept-5-2026.zip ~/restore-staging && rsync -a --delete --exclude='.worktrees/' ~/restore-staging/.agents/ ~/.agents/`, verify the restore (`unzip -t ~/.agents-backup-sept-5-2026.zip` exit 0 + manifest match); and IF the stack-check plist was already bootout-ed/migrated per 3.1 [AMEND-F4] THEN the plist RESTORE is SCOPED [AMEND-R5-8]: PRE-3.3 failures = FIRST run `launchctl bootout gui/$UID/com.flightlead.check` [AMEND-R4-8], THEN re-load the OLD plist KEPT IN PLACE, BYTE-UNTOUCHED, at ~/Library/LaunchAgents/com.agents.stack-check.plist (never deleted before the 3.2 gate per 3.1; the migrated config lives under the NEW label com.flightlead.check.plist so the original stays pristine); POST-3.3 failures = restore = re-load com.flightlead.check.plist (the OLD plist is DELETED in 3.3 and the backup zip EXCLUDES ~/Library/LaunchAgents — verified 0 zip entries — so the old-plist restore path no longer exists POST-3.3); then re-run from that checkpoint
- [ ] #12 #12 #12 IF a tag or publish is attempted THEN CI SHALL block it unless the npm name `flightlead` is claimed, the 0.4 voice-pass and 0.5 tickets are closed — closure mechanically observed via the publish-gate marker file .github/publish-gate [AMEND-F9], flipped ONLY on 0.4+0.5 closure and required by the CI publish gate — and scripts/privacy-audit is green; SCOPE NOTE [AMEND-R4-3]: privacy-audit green covers the GREP only (backlog/ paths are a declared exclusion per 1.12); the backlog/ check — the npm package SHALL NOT contain backlog/ — is the SEPARATE 0.5 deliverable and gates publish through 0.5 closure, not through the grep; and WHEN CI runs on main THEN it SHALL execute typecheck, biome, tests, semgrep (telemetry off, pinned rulesets), the frontmatter license/upstream check, and the dist build producing the bin-referenced output (per Step 1.11b)
- [ ] #13 #13 #13 BEFORE `rm -rf ~/.agents/.git` the post-port tree state SHALL be committed on ~/.agents main (Step 3.2-pre) and the Step 3.2 checkpoint SHALL pass: `git status --porcelain` empty in ~/.agents AND in .worktrees/TASK-45 (satisfiable via the 3.2-pre commit PLUS the 3.2-pre removal of the worktree's redundant untracked TASK-45 file [AMEND-F3]), backup restore-verified per AC#11 (incl. the 0.9-generated manifest match), npm test green, sync no-op exit 0 from flightlead, backlog task list functional from flightlead (Step 3.2 gate (e) [AMEND-R5-16]), stack-check plist migrated per 3.1 with :8788 healthy via doctor, and state/logs/reports relocated to ~/.local/state/flightlead/ (performed in 3.2-pre per [AMEND-R4-6])
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 All 13 acceptance criteria verified with fresh evidence (command output + exit codes from the executing session)
- [ ] #2 Steps 0-4 executed in order with GATE 0 and GATE 3.2 pre-destruct checkpoints passed and recorded
- [ ] #3 Post-flip terminal state matches AC#5's two anchored lists exactly (verified by ls of ~/.agents and $HOME)
- [ ] #4 v0.1.0 stranger test passes per AC#6 (npm pack global install on clean arm64, doctor exit 0)
- [ ] #5 Public flip NOT performed as part of this task — PII audit precondition (Step 0.5) remains open for the flip task
- [ ] #6 HITL gate satisfied: full 1.13 diff review presented to the user with explicit approval, and GATE 3.2 pre-destruct checkpoint human-approved
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
PORT PLAN v8 (reviewer-amended from v7; round-5 findings 1-22 applied — see Implementation Notes amendment map; markers [AMEND-R5-1]..[AMEND-R5-21]; R5-22 verified no-op). Round-6 execution amendments [AMEND-R6-1..6] applied 2026-09-05 — see Implementation Notes.

GATE 0 (plan review — moved here from former AC#9): all four fresh plan-review subagents SHALL return a unanimous PASS on this checklist; verdicts recorded in Implementation Notes BEFORE Step 1 executes. EXIT CRITERIA: the Step 0.7 secrets decision AND the Step 0.8 docs-spine disposition SHALL both be recorded. BRANCH RULE [AMEND-R4-1]: ALL Step 0-1 port commits land on main (no port work on draft/v0.1.0-spec or any other branch). BRANCH-RULE EXTENSIONS [AMEND-R5-13][AMEND-R5-15]: docs-file must-fixes (README offline-claim, trademark line) route through 0.8 -> 0.8b merge ONLY — 0.1's corresponding item (4) applies post-merge or on the flightlead repo and is NOT duplicated on main [AMEND-R5-13]; and ALL flightlead repo commits (including 3.2-pre's src/ edits) land on main after 0.8b [AMEND-R5-15].

STEP 0 — Draft closure + repo hygiene:
0.1 Apply the NINE enumerated must-fixes from the all-4 reviewer docs pass: (1) package.json license metadata MIT -> Apache-2.0; (2) dangling research-doc citations fixed or removed; (3) dist build produced and committed via CI; (4) offline-claim in README/docs corrected (per [AMEND-R5-13]: routed through 0.8 -> 0.8b merge ONLY — applies post-merge or on the flightlead repo, NOT duplicated on main); (5) semgrep-ruleset redistribution clause added (invoked-not-bundled, LGPL note in ATTRIBUTION/NOTICE); (6) all REVIEWER placeholder comments resolved; (7) difit disposition recorded (deferred to a later milestone per brief "difit-later"); (8) relicense statement for harness-original skills (MIT pre-v0.1.0 -> Apache-2.0 from v0.1.0, frontmatter license updated at port); (9) LIVE-ROOT RELICENSE [AMEND-R4-5]: the live-root ~/.agents/LICENSE file itself is relicensed MIT -> Apache-2.0 as an explicit 0.1 action — item (8) covers ONLY skills frontmatter, and AC#5(a) cites this item (9).
0.2 in-my-voice stays personal (excluded from port; REMAINS at ~/.agents/skills/in-my-voice post-flip per the 1.2 host-skills path decision); tldraw-offline stays personal (excluded; it lives at ~/.claude/skills/tldraw-offline — NOT ~/.agents/skills — verified 2026-09-05 — and is untouched by the flip).
0.3 ATTRIBUTION.md + NOTICE completed: headroom Apache-2.0, opencode MIT, pi MIT, ComposioHQ fix, addyosmani + ponytail entries, trademark line ("flightlead name/logo (c) Shabib Hossain — not covered by the code license"). FULL PROVENANCE-TABLE PROMOTION [AMEND-R5-10]: skills/README.md is deleted in 3.3 — BEFORE that delete, 0.3 must enumerate the FULL provenance table into ATTRIBUTION.md (7 rows, incl. obra/superpowers and mattpocock/skills), capturing what the README held before its deletion. RUN-TIME RE-VERIFY [AMEND-R6-4]: at execution time the 7-row ATTRIBUTION provenance table SHALL be re-verified against the live skills/README.md (re-count rows at run time) BEFORE the 3.3 delete.
0.4 DEFERRED TICKET (pre-publish): voice-pass of user-facing prose — MUST be closed before tag/publish (Step 4 precondition).
0.5 DEFERRED TICKET (pre-publish): full docs/research privacy sweep [AMEND-F8]: ALL notes under docs/research (50 dated notes + README as of 2026-09-05; enumerate at run time — no hardcoded file count) — MUST be closed before tag/publish (Step 4 precondition); only the two public-safe notes named in Step 1.5 port. FATE RULE [AMEND-R5-11]: ONLY the two named public-safe notes port (Step 1.5); ALL remaining notes (~48, enumerate at run time) STAY PRIVATE and are excluded from the port; 0.5 done-state = sweep report green + fates recorded for every note. SEPARATE BACKLOG-PACKAGE CHECK [AMEND-R4-3]: verifying the published npm package does NOT contain backlog/ (files allowlist + published-tarball inspection) is a SEPARATE 0.5 deliverable, DISTINCT from the scripts/privacy-audit grep (which only EXCLUDES backlog/ paths per 1.12) — the two checks are not the same gate. PUBLIC-FLIP PII AUDIT — PUBLIC-FLIP PRECONDITION (accepted from round-6 kimi MED): before the flightlead GitHub repo goes public, a full repo PII audit must pass: (a) multi-scout divide-and-conquer scan of ~/Projects/flightlead working tree, git history of ALL branches (main, draft/v0.1.0-spec, archive/loopeng-history), ported backlog/, and ignored-but-present files; (b) 3 analyze→fix→review loop passes (same pattern as the TASK-45 plan gate); (c) categories scanned: identity strings (real name + /Users/shabibhossain absolute paths, personal emails, hostnames — NOT the chosen public handles shabib87/codewithshabib, which are brand and allowed), secrets/credentials incl. anything committed in history, private infrastructure (private repo URLs, proxy/LaunchAgent details), pre-rename 'loopeng' references, personal content (in-my-voice, career/CV material, backlog decision text, competitor-strategy notes). Git history cannot be fixed by file deletion — scrub (git filter-repo / clean squash) or repo-stays-private is the mitigation. Repo remains PRIVATE until this audit passes. PUBLISH-GATE SCOPE: the publish-gate marker (0.5 closure before tag/publish) covers the research sweep + backlog npm-package check only; the PUBLIC-FLIP PII AUDIT gates the repo flip exclusively and does not block publish.
0.6 Hygiene set (now, publish-gating): CONTRIBUTING.md, SECURITY.md, issue templates, CI (EXTENDS [AMEND-R5-17] the existing .github/workflows/ci.yml arriving via the 0.8b merge — verified to lack the semgrep/frontmatter/dist steps — by ADDING those: typecheck + biome + tests + semgrep telemetry-off with pinned rulesets + frontmatter license/upstream check; NOT built fresh), branch protection on main, ASCII wordmark, trademark line, CHANGELOG.md (release-please) [AMEND-R4-12]. CI gates publish. CI TIMING (round-3): every tests/dist CI step is SKIP-WHEN-ABSENT — the CI steps detect that tests/ or the dist build do not yet exist (they arrive in Step 1) and pass with a skip notice, so branch protection enabled at 0.6 does NOT block the 0.8 docs-spine merge; full tests/dist gating turns on after Step 1.13.
0.7 SECRETS LOCATION (GATE-0 EXIT CRITERION): decide + record the secrets location & injection mechanism — .env-only (live root, git-ignored) + committed .env.example; zero secret values in payload templates; init/sync inject personal values from .env at template-resolve time. Recorded BEFORE GATE 0 passes; unblocks 1.1b de-personalization.
0.8 DOCS SPINE: finalize the product docs spine (NORTH_STAR.md / PRODUCT.md / ROADMAP.md / README.md) on the flightlead branch draft/v0.1.0-spec — including the Step 0.1 must-fixes (offline-claim correction, trademark line). Disposition: IN-SCOPE for v0.1.0; a deferral would require an explicit recorded ticket BEFORE GATE 0 (none granted). (The merge to main is Step 0.8b.)
0.8b BRANCH MERGE [AMEND-R4-1]: after 0.8, merge draft/v0.1.0-spec into main so main carries the docs spine BEFORE any Step 1 port work; from this point ALL Step 0-1 port commits land on main. ARTIFACT SOURCING [AMEND-R5-14]: the step-0 artifacts arriving VIA the draft->main merge (verified present on draft): LICENSE, NOTICE, ATTRIBUTION seed, CONTRIBUTING, SECURITY, issue templates, README spine; NET-NEW on main (added by 0.6/0.9, not in the merge): the .github/workflows/ci.yml extension, publish-gate marker wiring, backup manifest — stated so the merge avoids conflicts on those files.
0.9 BACKUP MANIFEST (pre-Step-1 action): generate ~/backups/agents-backup-sept-5-2026.manifest.txt via `unzip -l ~/.agents-backup-sept-5-2026.zip > ~/backups/agents-backup-sept-5-2026.manifest.txt` BEFORE Step 1 (the manifest does not exist yet — verified 2026-09-05), so the GATE 3.2(b) "manifest match" is satisfiable.
0.9b ZSHRC BASELINE (numbered pre-Step-1 action) [AMEND-R6-3]: record sha256 of ~/.zshrc to ~/backups/zshrc.sha256.pre-step-1.txt via `shasum -a 256 ~/.zshrc > ~/backups/zshrc.sha256.pre-step-1.txt` BEFORE Step 1; AC#8's zshrc verification cites this numbered step.
0.10 PRE-FLIP CLEANUP: delete the dangling ~/.claude/skills/sh-humanizer symlink (it targets the nonexistent ~/.agents/skills/sh-humanizer — verified dangling 2026-09-05).

STEP 1 — Port payload, TWO PARALLEL STREAMS:
Stream A (content):
1.1a COPY config/ -> payload/config/ verbatim (mechanical copy, no edits).
1.1b DE-PERSONALIZE the copies: template hardcoded /Users/shabibhossain/... paths in config/agents/researcher.md and config/agents/implementer.md (plus any further hits 1.12 finds); personal values become template variables resolved at init. BLOCKED BY the Step 0.7 secrets-location decision (see ENABLERS) — the extraction destination (.env-only + .env.example) must be recorded before templating.
1.2 skills/ -> payload/skills/, excluding in-my-voice and tldraw-offline (personal). DIAGRAM-DESIGN DISPOSITION (recorded, NOT vendored): the real link target is ../../code/diagram-design/skills/diagram-design; the symlink is an INIT-TIME HOST ARTIFACT created on the target machine, NOT payload content — the payload excludes both the link and all diagram-design content. init ships a symlink/install policy: create the host link only when the target ../../code/diagram-design/skills/diagram-design exists, else skip + doctor warning. NOTICE/ATTRIBUTION gains the diagram-design entry. No symlinked content and no payload-internal symlink is committed; tests assert the init policy, not payload symlink contents. HOST SKILLS PATH (single, authoritative — round-3): ~/.agents/skills/ — AC#5, AC#10, this step, and Step 3.3 all name ~/.agents/skills/ and nothing else; personal in-my-voice REMAINS at ~/.agents/skills/in-my-voice post-flip; tldraw-offline is NOT part of this disposition (it lives at ~/.claude/skills/tldraw-offline and is untouched).
1.4 docs/architecture/ -> docs/architecture/ (updated to the post-loopeng model).
1.9 AGENTS.md constitution -> payload/AGENTS.md [AMEND-R5-5, superseding the R4-2 "static payload copy" reading]: TEMPLATED PER THE BRIEF — the brief's port matrix row says "PORT -> payload template", so AGENTS.md is a payload TEMPLATE whose ~/.agents path refs (bin/src/config-sync.ts, docs/architecture/worktree-discipline.md, AGENT-STACK-RUNBOOK.md — all deleted/relocated by 3.3) are REWRITTEN to flightlead equivalents at init time [AMEND-R5-5]; zero .env variables injected (path templating only, no secret templating); AC#4's byte-identity example remains payload/config/opencode.jsonc ONLY.
Stream B (machinery):
1.3 bin/src/*.ts + bin/test -> src/ + tests/; MIGRATE bun:test -> node:test (recorded rationale: flightlead package.json has no bun dependency; `node --import tsx --test` keeps the toolchain node-only) and wire all ported suites into npm test = `node --import tsx --test "tests/**/*.test.ts"`. ENUMERATE ALL 15 bin/test suites and map each into tests/ (or record a per-suite disposition); AC#3 is "every ported suite passes", not a named subset.
1.3b plugins/enforce.ts + verify-gate.ts -> src/hooks/ (verify one gate loads in opencode).
1.7 stack-versions.json -> flightlead.json manifest seed (per-tool install channel + version + doctor check id; completeness rule enforced by check).
1.10 RUNBOOK DECOMPOSITION: AGENT-STACK-RUNBOOK.md (2,267 lines) — per the brief's rule every section gets exactly one fate: script (init/check/doctor/update logic) / architecture doc (docs/architecture/<topic>.md) / payload template / delete. Brief mappings: fresh-Mac checklist + Phase 0 -> init; Phase 1 -> doctor checks + docs/architecture/headroom-proxy.md (cache-mode rationale, pricing-sync why, :8788 single-owner); Phase 2 -> payload/opencode.jsonc template + doctor config-parse; Phase 3 -> payload/agents templates; Phase 4 gotchas -> init symlink logic; pricing sync -> src/sync-model-pricing.ts + doctor; model tiers -> payload models.json default + docs/architecture/model-routing.md; safe-update procedures -> update; verification battery -> doctor subchecks; prune rules -> backlog tasks with triggers; carry-over manifest -> this port; quick reference -> README quickstart + docs/cli.md. Commit the full section->fate map; ZERO sections remain prose-only; replace the runbook with a pointer stub; STUB FATE [AMEND-R5-6]: the stub lives in the LIVE ROOT (~/.agents/AGENT-STACK-RUNBOOK.md) and is DELETED in Step 3.3 (the payload carries the decomposed docs, not the stub).
1.11 CLI BUILD (all-4-reviewer CRITICAL; completes BEFORE the Step 4 stranger test): extend src/cli/index.ts [AMEND-R4-4] ("src/cli.ts" was wrong — bin currently points at ./src/cli/index.ts; 1.11b repoints it to ./dist/cli/index.js [AMEND-R5-1]) with the brief's command surface: init, sync, update, check (incl. `check --stack-only` [AMEND-R5-3] — defined here so the pinned 3.1 launchd argv exists on the CLI surface), doctor, scaffold --project. flightlead.json manifest is CLI-managed; JSONL ledger appends at ~/.local/state/flightlead/ (every command logs files touched, decisions, errors, exit code; refusal = log line + non-zero exit — zero silent failure). DOCTOR NODE-PATH GUARD [AMEND-R6-2]: WHEN com.flightlead.check.plist is loaded/present, THEN doctor/check SHALL verify the plist-pinned node path /Users/shabibhossain/.nvm/versions/node/v22.23.1/bin/node exists and satisfies engines.node (>=20) — a future nvm upgrade must fail loudly at doctor time, not silently break the Sunday LaunchAgent (com.flightlead.check.plist); the guard is scoped to the plist being loaded/present so AC#6's clean-fixture doctor run (no plist) still exits 0. Acceptance criteria are tested against the BUILT commands.
1.11b BUILD/BUNDLE (publish prerequisite): package.json `bin` SHALL point at BUILT JS output in dist/ — never raw TS source requiring tsx at runtime (installed users have no tsx). Add the build step (tsc or tsup) producing dist/, wire it into CI and prepublishOnly; AC#6 gates the built output. ROUND-3 BUILD OUTPUT FIX: ALSO rewrite package.json `files` from the current ["src/"] to e.g. ["dist/", "payload/"] so dist/ is actually published. ROUND-3.5 BUILD CONFIG PIN [AMEND-F10]: the repo tsconfig is noEmit:true with no outDir/rootDir (verified) — PIN the exact build config: add tsconfig.build.json with outDir dist/ and rootDir src/, emitting ./dist/cli/index.js exactly matching the package.json `bin` path (repoint bin to ./dist/cli/index.js) — no open either/or; the entrypoint path is pinned.
1.12 PRIVACY-AUDIT ARTIFACT: check in scripts/privacy-audit containing the exact grep command + the checked-in personal-strings list (shabibhossain, @loopeng, /Users/shabibhossain paths, personal secrets paths; shabib87/codewithshabib excluded from personal-strings list per [AMEND-R6-6] brand ruling — grepping it would false-fail AC#2's privacy audit at 1.13) + declared exclusion paths (NOTICE copyright lines, README trademark line, and the backlog/ copy per the 1.6 privacy note [AMEND-F6]). SCOPE NOTE [AMEND-R4-3]: the backlog/ EXCLUSION here applies to THIS grep only; the backlog/ npm-package-exclusion check is the SEPARATE 0.5 deliverable and is NOT part of privacy-audit green. Running this artifact IS AC#2.
Shared: 1.5 swept research notes -> docs/research/: EXACTLY TWO public-safe notes [AMEND-F8] — docs/research/2026-09-02-oss-agent-harnesses.md (competitor scan) and docs/research/2026-09-02-loopeng-moat-analysis.md (moat analysis). The third referenced note, docs/research/2026-09-03-loopeng-packaged-distribution.md, STAYS PRIVATE (never ported; explicit fate recorded here per the References list).
Verify (1.13): scripts/privacy-audit green; npm test green; human reviews the full diff.
Switchover (1.6 — moved AFTER 1.13 passes, per safety; COPY-BASED per round-3): FIRST in ~/Projects/flightlead run `git checkout main` — this happens AFTER the 0.8b merge [AMEND-R4-1] has landed the docs spine on main, so the checkout does NOT strand the port: main is the working branch and ALL Step 0-1 port commits land on main [AMEND-R4-1] (the repo currently sits on draft/v0.1.0-spec). THEN COPY backlog/ -> flightlead/backlog/ (incl. driver chain TASK-3..10) — do NOT move or delete the source. PRIVACY [AMEND-F6 + AMEND-R4-3]: the backlog/ copy carries personal history (verified hits: shabibhossain, codewithshabib, @loopeng in task-45) — backlog/ joins the 1.12 declared-EXCLUSION list for the grep, while the check that the npm package does NOT contain backlog/ is the SEPARATE 0.5 deliverable. IMMEDIATELY after the copy, verify `backlog task list` succeeds from BOTH ~/Projects/flightlead AND ~/.agents (dual-sided check: the ~/.agents source is still present so the CLI still works there — a MOVE would make `backlog task list` from ~/.agents error with "No Backlog.md project found", verified by round-2 reviewers). From this point backlog CLI + conductor ops run from ~/Projects/flightlead; the copy-left ~/.agents/backlog/ source is deleted ONLY in the Step 3.3 deletion list.

STEP 2 — sync proven: sandbox run (flightlead sync against a scratch live-root, exit 0) -> one-file live test (byte-identical, per AC#4) -> full materialization.

STEP 3 — Flip (checkpoint-ordered):
3.1 LAUNCHAGENT FACTS + DISPOSITIONS: com.headroom.proxy.plist targets ~/.local/bin/headroom (pipx-managed) — it does NOT reference ~/.agents/bin, so it needs NO rewrite: leave it untouched, and add a doctor-verify check that :8788 stays healthy and launchd-owned across the flip. com.agents.stack-check.plist (Sunday 09:00) runs ~/.agents/bin/src/stack-check.ts, which breaks post-flip (bin/ is deleted in 3.3). Recorded disposition: MIGRATE [AMEND-F4] — write the migrated plist to a NEW label/path ~/Library/LaunchAgents/com.flightlead.check.plist, with ProgramArguments PINNED [AMEND-R4-15][AMEND-R5-2] to the exact argv: /Users/shabibhossain/.nvm/versions/node/v22.23.1/bin/node /Users/shabibhossain/Projects/flightlead/dist/cli/index.js check --stack-only (launchd has NO nvm PATH, so the ABSOLUTE node path is pinned — /usr/bin/env node would fail; node verified at ~/.nvm/versions/node/v22.23.1/bin/node); the Step 1.11 doctor node-path guard [AMEND-R6-2] verifies this pinned path exists and satisfies engines.node (>=20) BEFORE the plist is loaded (narrow stack-version check via the flightlead CLI; if the Step 1.11 check surface lands a different flag name, update this argv BEFORE the plist is loaded) AND StandardOutPath/StandardErrorPath log paths (currently ~/.agents/reports/stack-check-launchd.log — verified 2026-09-05) repointed to ~/.local/state/flightlead/; `launchctl bootout` the old com.agents.stack-check.plist but leave the old plist FILE BYTE-UNTOUCHED at ~/Library/LaunchAgents/ until Step 3.3 deletes it (the backup zip root is .agents/ only and does NOT cover ~/Library/LaunchAgents — verified 0 zip entries — so the old plist is the rollback artifact AC#11 promises); BEFORE loading the new plist, run `mkdir -p ~/.local/state/flightlead` (the migrated com.flightlead.check.plist StandardOut/ErrorPath logs live there and the dir does not exist yet at that point — verified 2026-09-05) [AMEND-R6-1]; load the NEW com.flightlead.check.plist; keep the Sunday 09:00 schedule; verify via doctor. The old com.agents.stack-check.plist FILE is deleted in 3.3; plist restore across a checkpoint failure = bootout com.flightlead.check then re-load the BYTE-UNTOUCHED original com.agents.stack-check.plist, covered in AC#11 (PRE-3.3 only — POST-3.3 restore = re-load com.flightlead.check.plist per [AMEND-R5-8]).
3.2 PRE-DESTRUCT CHECKPOINT — first, 3.2-pre: (i) ensure TASK-45's canonical copy lives in flightlead/backlog/ (guaranteed by the 1.6 COPY), THEN ALSO remove (rm) the .worktrees/TASK-45 redundant untracked copy of the TASK-45 backlog file [AMEND-F3] — the worktree copy is UNTRACKED (verified `??` on branch task/TASK-45 in ~/.agents/.worktrees/TASK-45), so it cannot be cleared by the main-tree 3.2-pre commit and there is nothing to merge from the worktree; the canonical copy in flightlead/backlog/ supersedes it — so that GATE (a)'s worktree porcelain check is satisfiable; gate (a) is scoped accordingly (main-tree commit + this worktree copy removal); (ii) PERFORM THE RELOCATION [AMEND-R4-6] (GATE (f) verifies it; this step OWNS it): run `mkdir -p ~/.local/state/flightlead && mv ~/.agents/state/* ~/.agents/logs/* ~/.agents/reports/* ~/.local/state/flightlead/`, AND repoint the ported config-sync default state path in src/ from ~/.agents/state/config-materialize.json to ~/.local/state/flightlead/, AND repoint the agents-install.ts install-state default (~/.agents/state/install-state.json, bin/src/agents-install.ts:147) to ~/.local/state/flightlead/ [AMEND-R5-9] — BEFORE the gates below run; (iii) COMMIT THE POST-PORT TREE STATE on ~/.agents main (all remaining changes; the copy-left ~/.agents/backlog/ tree is deleted per 3.3); this makes the porcelain-empty gate satisfiable. Then ALL must pass before `rm -rf ~/.agents/.git`: (a) `git -C ~/.agents status --porcelain` and `git -C ~/.agents/.worktrees/TASK-45 status --porcelain` both return empty (satisfiable via the 3.2-pre commit + the worktree redundant-copy removal); (b) backup restore-verified per AC#11 (`unzip -t ~/.agents-backup-sept-5-2026.zip` exit 0 + match against the 0.9-generated manifest at ~/backups/agents-backup-sept-5-2026.manifest.txt); (c) npm test green in flightlead; (d) sync no-op exit 0 from flightlead; (e) backlog task list functional from flightlead; (f) state/logs/reports relocated to ~/.local/state/flightlead/ per the brief (performed in 3.2-pre item (ii) [AMEND-R4-6]); (g) :8788 healthy via doctor check + stack-check plist migrated per 3.1 (new com.flightlead.check.plist loaded, old plist byte-untouched).
3.3 THEN remove ~/.agents/.git. ORDER: first `git worktree remove --force ~/.agents/.worktrees/TASK-45`, THEN `rmdir ~/.agents/.worktrees` [AMEND-R4-9] (remove the now-empty worktree parent dir), then remove .git. DELETION SUB-STEP (run only AFTER 3.2 passes; ported originals + copy-left + plist): delete ~/.agents/config/, ~/.agents/bin/, ~/.agents/plugins/, the ported docs/ (docs/architecture/ and the two ported docs/research/ note COPIES — non-ported personal research stays per AC#5), the ported skills (KEEP personal in-my-voice at ~/.agents/skills/ per the 1.2 single host-skills path; KEEP [AMEND-R5-7] the diagram-design host symlink ~/.agents/skills/diagram-design — AC#5(a)/AC#10 require it to REMAIN; tldraw-offline is at ~/.claude/skills/tldraw-offline and untouched), ~/.agents/skills/README.md (fate: DELETE, recorded [AMEND-R4-7]; its provenance table is promoted into ATTRIBUTION.md by 0.3 first per [AMEND-R5-10]), run `find ~/.agents -name .DS_Store -delete` [AMEND-R4-7] (.DS_Store verified present under ~/.agents/docs/, docs/spec/, and skills/), ~/.agents/backlog/ (the 1.6 copy-left source — delete ONLY here, after the dual-sided check and 3.2), ~/.agents/state/, ~/.agents/logs/, ~/.agents/reports/ [AMEND-F2 — all three so AC#5's "no state, no logs" is procedurally satisfiable; NOTE the mv in 3.2-pre already relocated their contents [AMEND-R4-6], so these deletions clear the emptied dirs], ~/.agents/stack-versions.json, ~/.agents/AGENT-STACK-RUNBOOK.md, and the old ~/Library/LaunchAgents/com.agents.stack-check.plist FILE (kept BYTE-UNTOUCHED in place through 3.2 per 3.1). BRIEF-MATRIX CONFLICT RESOLUTION [AMEND-F2]: the brief's port-matrix row "state/ STAY" is superseded by the post-flip terminal-state rule — AC#5's "no state, no logs" WINS; state/, logs/, and reports/ relocate to ~/.local/state/flightlead/ per the brief's state-location rule. .WORKTREES DISPOSITION [AMEND-F3 + AMEND-R4-9]: `git worktree remove --force ~/.agents/.worktrees/TASK-45` followed by `rmdir ~/.agents/.worktrees` — the previous "relocate .worktrees with .git intact until merged" disposition is IMPOSSIBLE because the worktree gitdir lives INSIDE ~/.agents/.git/worktrees/ (destroyed by `rm -rf ~/.agents/.git`), and task/TASK-45 carries ZERO unique commits (verified) — nothing to merge, so --force is correct. DONE-STATE: ~/.agents contains ONLY the enumerated AC#5 POST-FLIP ONLY-LIST.

STEP 4 — Publish (preconditions declared, in order): (1) npm name `flightlead` CLAIMED (registry currently 404s — squat vector; claim before ANY public announcement); (2) deferred tickets 0.4 (voice-pass) and 0.5 (docs/research privacy sweep PLUS the SEPARATE backlog-package-exclusion check [AMEND-R4-3]) CLOSED — mechanically observed via the PUBLISH-GATE MARKER FILE [AMEND-F9] .github/publish-gate in flightlead, flipped (committed) ONLY when both 0.4 and 0.5 are closed; CI blocks tag/publish unless the marker exists (chosen mechanism: marker file, NOT a protected GitHub environment); (3) scripts/privacy-audit green (grep scope ONLY — the backlog/ check is 0.5's separate deliverable, not part of this grep [AMEND-R4-3]); (4) CI publish-gate green. REPO VISIBILITY [AMEND-R4-14]: the flightlead repo STAYS PRIVATE at v0.1.0; the public flip is gated on the 0.5 sweep closing — no public announcement before that. THEN: stranger test on a clean arm64 macOS machine (flightlead init -> loadable config + flightlead.json manifest + JSONL ledger + doctor green) -> tag v0.1.0 -> npm publish (OIDC trusted publishing, release-please over conventional commits, CHANGELOG automatic). TASK-34 dissolution recorded: repo move + remote done; semver -> release-please; setup guide -> init/README. Driver chain TASK-3..10 resumes here, dogfooding under flightlead check.

ENABLERS (kept in-task, dispositioned):
- DECIDED IN 0.7 (GATE-0 exit criterion): secrets location & injection mechanism — .env-only (live root, git-ignored) + committed .env.example; recorded before 1.1b de-personalization.
- DECIDED (no ticket): backlog.md MCP wiring — NO MCP, CLI-only (recorded backlog-lifecycle.md:72, 2026-08-29).
- DECIDED (round-4 [AMEND-R4-1]): branch strategy — 0.8b merges draft/v0.1.0-spec into main; ALL Step 0-1 port commits land on main; the 1.6 `git checkout main` follows the 0.8b merge and does not strand the port. EXTENDED (round-5 [AMEND-R5-13][AMEND-R5-15]): docs must-fixes route via 0.8 -> 0.8b merge only; ALL flightlead repo commits (incl. 3.2-pre src/ edits) land on main after 0.8b.
- DECIDED (round-4 [AMEND-R4-14]): repo visibility — flightlead stays PRIVATE at v0.1.0; public flip gated on 0.5 sweep closure.
- DECIDED (round-4 [AMEND-R4-6]): relocation owner — 3.2-pre performs the state/logs/reports relocation (`mkdir -p ~/.local/state/flightlead && mv ...`) + the config-sync state-path repoint (extended round-5 [AMEND-R5-9]: agents-install.ts install-state default repointed too); GATE 3.2(f) verifies.
- DECIDED (round-4 [AMEND-R4-3]): backlog/ check scope — privacy-audit grep EXCLUDES backlog/; the npm-package-must-not-contain-backlog/ check is a SEPARATE 0.5 deliverable gating publish.
- DECIDED (round-3.5 [AMEND-F4]): LaunchAgent fates — com.headroom.proxy.plist untouched (no rewrite; :8788 health via doctor); com.agents.stack-check.plist MIGRATED in Step 3.1 to NEW label/path com.flightlead.check.plist (bootout only; old plist file kept BYTE-UNTOUCHED until 3.3, deleted in 3.3; ProgramArguments pinned per [AMEND-R4-15] with the ABSOLUTE nvm node path per [AMEND-R5-2]; StandardOut/ErrorPath repointed to ~/.local/state/flightlead/).
- DECIDED (round-3.5 [AMEND-F3]): .worktrees — `git worktree remove --force ~/.agents/.worktrees/TASK-45` in 3.3 followed by `rmdir ~/.agents/.worktrees` [AMEND-R4-9] (worktree gitdir lives inside ~/.agents/.git/worktrees/; zero unique commits on task/TASK-45; nothing to merge), BEFORE `rm -rf ~/.agents/.git`.
- DECIDED (round-3): host skills path — single path ~/.agents/skills/ (named consistently by AC#5, AC#10, Step 1.2, Step 3.3); personal in-my-voice REMAINS at ~/.agents/skills/in-my-voice; tldraw-offline untouched at ~/.claude/skills/tldraw-offline. Round-5 [AMEND-R5-7]: 3.3 explicitly KEEPS the diagram-design host symlink.
- DECIDED (round-3): CI timing — skip-when-absent CI tests/dist steps at 0.6; branch protection from 0.6; full tests/dist gating after 1.13. Round-5 [AMEND-R5-17]: 0.6 CI EXTENDS the ci.yml arriving via the 0.8b merge, not built fresh.
- DECIDED (round-3): backlog/ switchover — COPY-based at 1.6 onto main after `git checkout main` following the 0.8b merge [AMEND-F7 + AMEND-R4-1] (dual-sided `backlog task list` check); ~/.agents/backlog/ source deleted only in 3.3; backlog/ excluded from the 1.12 grep and npm-package-checked per the separate 0.5 deliverable [AMEND-F6 + AMEND-R4-3].
- DECIDED (round-3.5 [AMEND-F9]): publish-gate mechanism — marker file .github/publish-gate flipped on 0.4+0.5 closure; CI checks the marker.
- DECIDED (round-5 [AMEND-R5-5]): AGENTS.md payload disposition — templated (brief matrix "PORT -> payload template" wins); ~/.agents path refs rewritten to flightlead equivalents at init time; runbook pointer stub (1.10) lives in the live root and is deleted in 3.3 [AMEND-R5-6].
- FOG, non-blocking: ~/.zshrc PATH (init documents it, doctor checks it); CLAUDE.md fate (does not exist today; stays instance if recreated; claude code is milestone 0.3); docs/tbd fate (does not exist today; stays instance if recreated).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
- 2026-09-05: TASK-45 refined from decision map into single execution task; 45.1-45.7 removed (their answers live in the brief).
- 2026-09-05 reviewer pass #1 on docs (4/4 PASS-WITH-FIXES, 40 findings -> 20 deduped): must-fixes now enumerated concretely in Step 0.1.
- 2026-09-05 PORT PLAN v2 FAILED 4-reviewer round (not unanimous). Amendments A-K applied into PORT PLAN v3: (A) CLI build = Step 1.11 + AC#6; (B) runbook decomposition = Step 1.10 + AC#9; (C) diagram-design disposition = Step 1.2 + AC#10; (D) privacy-audit artifact + copy/de-personalize split = Steps 1.1a/1.1b/1.12 + AC#2; (E) test-runner alignment = Step 1.3 + AC#3; (F) Step 3 checkpoint + LaunchAgent-first = Steps 3.1/3.2 + AC#13; (G) hygiene = Step 0.6 + AC#12; (H) publish gating = Step 4 + AC#12; (I) AC fixes: closed post-flip file list = new AC#5, real host paths only = new AC#8 (nonexistent auth/secrets paths dropped), recovery protocol = new AC#11 (replaces untestable "no data loss"), plan-review gate moved to GATE 0 (replaces self-referential v2 AC#9); (J) must-fixes enumerated = Step 0.1; (K) enabler dispositions below.
- Recorded decisions: diagram-design = EXCLUDE content from payload + ship init-time host symlink/install policy + NOTICE/ATTRIBUTION entry (not vendored). Test runner: MIGRATE bun:test -> node:test (flightlead package.json has no bun; node --import tsx --test keeps the toolchain node-only). MCP = none, CLI-only (backlog-lifecycle.md:72). Attribution = code-taken-only + Addy Osmani shoutout; research inspirations not attributed.
- LaunchAgents (facts corrected in round 2): com.headroom.proxy.plist targets ~/.local/bin/headroom (pipx) — it never referenced ~/.agents/bin, so NO rewrite; :8788 health verified via doctor across the flip. com.agents.stack-check.plist (Sunday 09:00, runs ~/.agents/bin/src/stack-check.ts) = MIGRATE: repoint to the built flightlead check (~/Projects/flightlead/dist/ CLI), bootout + delete old plist, load migrated plist, keep Sunday 09:00 schedule.
- ENABLER DISPOSITIONS: secrets location — DECIDED IN 0.7 (.env-only + .env.example; GATE-0 exit criterion; unblocks 1.1b). MCP wiring — decided (none). LaunchAgents — decided (per 3.1). .worktrees — decided (relocate to ~/.agents-worktrees/ with .git intact until merged, then delete; 3.3). ~/.zshrc PATH — fog, non-blocking (init docs + doctor check). CLAUDE.md fate — fog, non-blocking (stays instance; claude code is 0.3). docs/tbd fate — fog, non-blocking (stays instance until decided).
- GATE 0 (replaces former AC#9): all four fresh plan-review subagents SHALL return unanimous PASS on this plan; verdicts recorded here before Step 1 executes. Exit criteria: 0.7 secrets decision + 0.8 docs-spine disposition recorded.
- tldraw-offline: removed from port + dependency; stays personal. A verified backup of ~/.agents exists (human-made, 2026-09-05: ~/.agents-backup-sept-5-2026.zip); rollback = restore backup per AC#11 protocol.
- 2026-09-05 PORT PLAN v4: round-2 findings 1-10 applied. (1) LaunchAgent facts corrected — proxy plist = ~/.local/bin/headroom (pipx), no rewrite + :8788 doctor check; stack-check plist disposition = MIGRATE (Step 3.1); AC#8 rewritten. (2) diagram-design link target corrected to ../../code/diagram-design/skills/diagram-design; link = init-time host artifact; AC#10 tests the policy, not payload contents. (3) AC#3 = every suite ported under tests/**/*.test.ts passes; Step 1.3 maps all 15 bin/test suites (or per-suite disposition). (4) New Step 0.7: secrets location & injection mechanism decided + recorded (.env-only + .env.example) as a GATE-0 exit criterion. (5) Step 3.3 explicit deletion sub-step for ported originals (config/, bin/, plugins/, ported docs/, ported skills, stack-versions.json, AGENT-STACK-RUNBOOK.md); AC#5 = enumerated $HOME-anchored post-flip list incl. .pi/ and .headroom/ staying; .worktrees disposition = relocate outside ~/.agents with .git intact until merged, then delete. (6) GATE 3.2(a) porcelain-empty made satisfiable via new 3.2-pre: commit post-port tree state on ~/.agents main before the checkpoint. (7) AC#11 concrete: backup ~/.agents-backup-sept-5-2026.zip, manifest ~/backups/agents-backup-sept-5-2026.manifest.txt, exact ditto+rsync restore command. (8) New Step 1.11b build/bundle: package.json bin points at built dist/ JS, never raw TS needing tsx; gated in AC#6 + AC#12. (9) New Step 0.8: docs spine (NORTH_STAR/PRODUCT/ROADMAP/README) finalized from flightlead branch draft/v0.1.0-spec incl. the 0.1 must-fixes, merged to main; no deferral. (10) Step 1.6 backlog migration moved AFTER 1.13 with immediate dual-sided `backlog task list` verification from both ~/Projects/flightlead and ~/.agents.

- 2026-09-05 PORT PLAN v5 (round-3 amendment map, findings 1-9 applied): (1) AC#5 rewritten as dual-anchored lists — POST-FLIP ONLY-LIST (every real survivor given an explicit fate: AGENTS.md keep, .gitignore keep, LICENSE keep+relicense Apache-2.0, docs/AUTHORING.md keep, docs/plans/ keep, docs/spec/ keep, docs/research/ non-ported notes keep + ported note COPIES deleted in 3.3, skills/ host dir keep, backlog/ copy-left deleted in 3.3, reports/ added to the negative list) + untouched $HOME dirs (~/.pi, ~/.headroom, ~/.claude/ incl. tldraw-offline); CLAUDE.md and docs/tbd/ marked DO-NOT-EXIST (verified absent) — drop-if-present. (2) Step 1.6 made COPY-based: copy backlog/ -> flightlead/backlog/, dual-sided `backlog task list` check with the source still present; ~/.agents/backlog/ deletion moved to the 3.3 list. (3) Step 3.2-pre rewritten: canonical TASK-45 copy lives in flightlead/backlog/ after the 1.6 COPY (the worktree copy is UNTRACKED, verified `??` — no merge from worktree); commit the materialized post-port tree on ~/.agents main; copy-left backlog/ deleted per 3.3. (4) Step 3.1: StandardOutPath/StandardErrorPath repointed to ~/.local/state/flightlead/; bootout ONLY — old plist file KEPT until 3.2 passes (backup zip root = .agents/ only, verified 0 LaunchAgents entries), deleted in 3.3; plist restore noted in AC#11. (5) Host skills path unified to ~/.agents/skills/ across AC#5, AC#10, Step 1.2, Step 3.3; in-my-voice REMAINS at ~/.agents/skills/in-my-voice; tldraw-offline exclusion corrected to ~/.claude/skills/tldraw-offline (verified live there); dangling ~/.claude/skills/sh-humanizer symlink cleanup added as Step 0.10. (6) CI timing: skip-when-absent tests/dist CI steps at 0.6 so branch protection cannot block the 0.8 docs-spine merge; full gating after 1.13. (7) Step 1.11b: package.json `files` rewritten from ["src/"] to ["dist/", "payload/"]; dist build emits the exact bin entrypoint path (bin -> ./src/cli/index.ts verified) or states the restructure before GATE 0. (8) Step 0.9 added: pre-Step-1 generation of ~/backups/agents-backup-sept-5-2026.manifest.txt via `unzip -l` (manifest verified absent) so GATE 3.2(b) manifest match is satisfiable. (9) AC#4 rewritten: byte-identity test named against a NON-TEMPLATED payload file (config-sync.ts); templated files get resolved-value assertions instead of raw-template byte-identity.

- 2026-09-05 PORT PLAN v6 (round-3.5 amendment map, findings 1-10 applied): [AMEND-F1] AC#5(a) phantoms removed — ".pi/ (stays); .headroom/ (stays)" dropped from the POST-FLIP ONLY-LIST (both are $HOME-level dirs, verified absent from ~/.agents; they remain ONLY in (b) untouched-$HOME list). [AMEND-F2] Step 3.3 deletion sub-step now includes ~/.agents/state/ and ~/.agents/logs/ alongside reports/ (AC#5 "no state, no logs" procedurally satisfiable); brief-matrix conflict resolution recorded: brief port-matrix row "state/ STAY" is superseded — post-flip terminal-state rule wins; state/logs/reports relocate to ~/.local/state/flightlead/. [AMEND-F3] Step 3.2-pre now ALSO removes (rm) the .worktrees/TASK-45 redundant untracked TASK-45 file (canonical copy in flightlead/backlog/ after the 1.6 COPY; worktree copy verified `??`, nothing to merge) and GATE (a) is scoped to main-tree commit + this removal; .worktrees disposition replaced — "relocate with .git intact" is impossible because the worktree gitdir lives inside ~/.agents/.git/worktrees/ and task/TASK-45 has zero unique commits (verified) — now `git worktree remove --force` in 3.3 before `rm -rf ~/.agents/.git`. [AMEND-F4] Step 3.1 plist migration now writes a NEW label/path com.flightlead.check.plist (ProgramArguments -> built dist/ CLI, logs -> ~/.local/state/flightlead/), leaving com.agents.stack-check.plist BYTE-UNTOUCHED until 3.3 deletes it — the old plist IS the rollback artifact; AC#8 + AC#11 updated (restore = re-load byte-untouched original). [AMEND-F5] AC#4 example fixed — config-sync.ts ports to src/ per the matrix and sync never materializes it; replaced with non-templated PAYLOAD files payload/config/opencode.jsonc / payload/AGENTS.md (byte-identity there; resolved-value assertions for templated files). [AMEND-F6] Backlog copy privacy — backlog/ added to the 1.12 declared-exclusion list; 0.5 pre-publish sweep now mirrors against backlog/ strings (verified personal hits: shabibhossain, codewithshabib, @loopeng in task-45); AC#2 + 1.6 updated. [AMEND-F7] Step 1.6 now specifies `git checkout main` in ~/Projects/flightlead (currently on draft/v0.1.0-spec) after the 0.8 spine merge, BEFORE the copy. [AMEND-F8] Research sweep count fixed — "48-file" replaced with "ALL notes under docs/research (50 dated notes + README; enumerate at run time)" in 0.5/Step 4; Step 1.5 names the exact two public-safe notes (2026-09-02-oss-agent-harnesses.md competitor scan + 2026-09-02-loopeng-moat-analysis.md moat analysis) and gives the third (2026-09-03-loopeng-packaged-distribution.md) the explicit fate STAYS PRIVATE. [AMEND-F9] Publish-gate mechanism specified — marker file .github/publish-gate flipped on 0.4+0.5 closure, required by the CI publish gate (chosen over protected GitHub environment); AC#12 + Step 4 updated. [AMEND-F10] 1.11b build config pinned — tsconfig.build.json with outDir dist/, rootDir src/ (repo tsconfig is noEmit:true, no outDir/rootDir — verified), emitting ./dist/cli/index.js exactly matching package.json bin; the prior either/or is closed.

- 2026-09-05 PORT PLAN v7 (round-4 amendment map, findings 1-15 applied): [AMEND-R4-1] BRANCH STRATEGY — new Step 0.8b merges draft/v0.1.0-spec into main after 0.8 (0.8 no longer claims the merge itself); BRANCH RULE recorded at GATE 0 + ENABLERS: ALL Step 0-1 port commits land on main; 1.6's `git checkout main` realigned to follow the 0.8b merge so it cannot strand the port. [AMEND-R4-2] AC#4 vs 1.9 — AC#4 byte-identity example is now payload/config/opencode.jsonc ONLY (payload/AGENTS.md removed from the example); 1.9 reworded to "payload/AGENTS.md static payload copy" (not a template). [AMEND-R4-3] 1.12 vs AC#12 — the backlog/ npm-package-exclusion check (npm package SHALL NOT contain backlog/) is now a SEPARATE 0.5 deliverable, distinct from the privacy-audit grep (which only excludes backlog/ paths); 0.5, 1.12, 1.6, Step 4(3), AC#12, and the backlog-switchover enabler all updated; the old "0.5 sweep mirrors against backlog/ strings" framing removed. [AMEND-R4-4] 1.11 — "implement src/cli.ts" fixed to "extend src/cli/index.ts" (build emits dist/cli/index.js; package.json bin verified). [AMEND-R4-5] LIVE-ROOT LICENSE — 0.1 gains item (9): live-root ~/.agents/LICENSE relicensed MIT -> Apache-2.0 (item 8 covers only skills frontmatter); AC#5(a) LICENSE fate now cites Step 0.1 item (9). [AMEND-R4-6] RELOCATION OWNER — 3.2-pre item (ii) now PERFORMS the relocation: `mkdir -p ~/.local/state/flightlead && mv ~/.agents/state/* ~/.agents/logs/* ~/.agents/reports/* ~/.local/state/flightlead/` plus repointing the ported config-sync default state path (~/.agents/state/config-materialize.json -> ~/.local/state/flightlead/); GATE 3.2(f) verifies; 3.3 deletions clear the emptied dirs; AC#13 notes the owner. [AMEND-R4-7] 3.3 gains `find ~/.agents -name .DS_Store -delete` (.DS_Store verified under ~/.agents/docs/, docs/spec/, skills/) and records ~/.agents/skills/README.md fate = DELETE. [AMEND-R4-8] AC#11 plist restore now runs `launchctl bootout gui/$UID/com.flightlead.check` FIRST, before re-loading the byte-untouched original com.agents.stack-check.plist; 3.1 restore note aligned. [AMEND-R4-9] WORKTREE PARENT — 3.3 appends `rmdir ~/.agents/.worktrees` after `git worktree remove --force`; .worktrees enabler updated. [AMEND-R4-10] AC#9 — "section" defined as one H2 heading of AGENT-STACK-RUNBOOK.md, with a committed heading inventory as the checklist artifact. [AMEND-R4-11] AC#1 — inline asset->location mapping + personal exclusions (in-my-voice; tldraw-offline at ~/.claude/skills) now live IN AC#1, no deferral to the brief. [AMEND-R4-12] CHANGELOG.md (release-please) added to the 0.6 hygiene set. [AMEND-R4-13] AC#5(b) adds ~/.pi-subagents (brief matrix row; verified absent — N/A) to the untouched list. [AMEND-R4-14] REPO VISIBILITY — decision recorded (ENABLERS + Step 4): flightlead stays PRIVATE at v0.1.0; public flip gated on 0.5 sweep closure. [AMEND-R4-15] STACK-CHECK COMMAND — 3.1 pins the exact ProgramArguments argv for com.flightlead.check.plist: /usr/bin/env node /Users/shabibhossain/Projects/flightlead/dist/cli/index.js check --stack-only (update-before-load clause if the 1.11 flag name differs).

- 2026-09-05 PORT PLAN v8 (round-5 surgical amendment map, findings 1-22 applied): [AMEND-R5-1] 1.11 — false "bin verified pointing there" parenthetical struck; now records bin currently points at ./src/cli/index.ts and 1.11b repoints it to ./dist/cli/index.js. [AMEND-R5-2] 3.1 — launchd has no nvm PATH; com.flightlead.check.plist ProgramArguments now pin the ABSOLUTE node path /Users/shabibhossain/.nvm/versions/node/v22.23.1/bin/node (replacing /usr/bin/env node). [AMEND-R5-3] 1.11 — `check --stack-only` added to the check-command list so the pinned 3.1 argv is defined on the CLI surface. [AMEND-R5-4] AC#5(a) ONLY-LIST — .env (keep, unversioned, per Step 0.7) added; post-flip sync writes .env from 0.7's secrets mechanism. [AMEND-R5-5] 1.9 / AC#4 / AC#1 — payload/AGENTS.md re-classed as TEMPLATED per the brief matrix (PORT -> payload template): ~/.agents path refs (bin/src/config-sync.ts, docs/architecture/worktree-discipline.md, AGENT-STACK-RUNBOOK.md) rewritten to flightlead equivalents at init time; byte-identity example remains payload/config/opencode.jsonc ONLY. [AMEND-R5-6] 1.10 — runbook pointer-stub fate pinned: stub lives in the LIVE ROOT and is deleted in Step 3.3. [AMEND-R5-7] 3.3 deletion clause — KEEP the diagram-design host symlink (AC#5(a)/AC#10 require it to remain). [AMEND-R5-8] AC#11 — restore protocol SCOPED: old byte-untouched com.agents.stack-check.plist reload applies PRE-3.3 only; POST-3.3 restore = re-load com.flightlead.check.plist (old plist deleted in 3.3; backup zip excludes ~/Library/LaunchAgents). [AMEND-R5-9] 3.2-pre — agents-install.ts install-state default (~/.agents/state/install-state.json, bin/src/agents-install.ts:147) added to the ~/.local/state/flightlead/ repoint alongside config-sync's config-materialize.json. [AMEND-R5-10] 0.3 — FULL provenance-table promotion into ATTRIBUTION.md (7 rows incl. obra/superpowers and mattpocock/skills) enumerated BEFORE skills/README.md deletion in 3.3. [AMEND-R5-11] 0.5 — fate rule recorded: only the two named public-safe notes port (Step 1.5); ALL remaining notes (~48, enumerate at run time) STAY PRIVATE and are excluded; 0.5 done-state = sweep report green + fates recorded. [AMEND-R5-12] AC#9 — heading inventory now includes the pre-first-H2 preamble block (first H2 verified at line 24); inventory = every H2 + preamble. [AMEND-R5-13] GATE 0 branch rule — docs-file must-fixes (README offline-claim, trademark line) route through 0.8 -> 0.8b merge ONLY; 0.1 item (4) applies post-merge or on the flightlead repo, not duplicated on main. [AMEND-R5-14] 0.8b — artifact sourcing stated: LICENSE, NOTICE, ATTRIBUTION seed, CONTRIBUTING, SECURITY, issue templates, README spine arrive via the draft->main merge (verified on draft); CI extension, publish-gate wiring, backup manifest are net-new on main. [AMEND-R5-15] GATE 0 branch rule extended — ALL flightlead repo commits (including 3.2-pre's src/ edits) land on main after 0.8b. [AMEND-R5-16] AC#13 — missing gate (e) "backlog task list functional from flightlead" added (now all 7 gates a-g). [AMEND-R5-17] 0.6 — CI EXTENDS the existing .github/workflows/ci.yml arriving via the 0.8b merge (verified to lack semgrep/frontmatter/dist steps); no longer "build fresh". [AMEND-R5-18] AC#8 — unmeasurable checks replaced with a concrete command battery (backlog task list / flightlead doctor / flightlead check / git status --porcelain, all exit 0, from flightlead) + pre-Step-1 sha256 baseline of ~/.zshrc recorded to ~/backups/zshrc.sha256.pre-step-1.txt. [AMEND-R5-19] AC#6 — "real install" defined as `npm pack && npm i -g ./flightlead-*.tgz` on a clean arm64 fixture; "healthy" defined as `flightlead doctor` exiting 0 on that fixture. [AMEND-R5-20] Description — plan label bumped PORT PLAN v5 -> PORT PLAN v8. [AMEND-R5-21] AC#1 — inline mapping row added: backlog/ -> flightlead/backlog/ (COPY per 1.6; ~/.agents source deleted in 3.3). [AMEND-R5-22] VERIFIED NO-OP: CHANGELOG.md (release-please) already present in Step 0.6 via [AMEND-R4-12] — no change needed.

2026-09-05 CONFIRMATION ROUND (round 6, PORT PLAN v8, severity-threshold gate: READY iff zero HIGH/CRITICAL): UNANIMOUS 4/4 READY — deepseek ses_f8d57f52, glm ses_f8d57f51, kimi ses_f8d57f50, qwen ses_f8d57f50. All 4 spot-checked [AMEND-R5-1..21] landed in AC/step bodies. Zero HIGH/CRITICAL findings. Non-blocking MED/LOW notes recorded for execution: (a) post-3.3 zip restore must --exclude .env (secrets not in pre-flip backup) and re-run 3.3 (restore re-materializes .git); (b) backlog/ git-history scrub = public-flip precondition in 0.5 (repo stays private at 0.1.0); (c) 3.1 needs mkdir -p ~/.local/state/flightlead before plist load; (d) engines.node >=20 vs pinned v22.23.1 — doctor should guard the pinned path; (e) zshrc sha256 baseline + provenance-row count = numbered pre-Step-1 actions; (f) brief matrix 'backlog/ MOVE' deviation to COPY+delete-in-3.3 recorded with rationale. AC#2 (all-4 reviewer consultation) evidence: this note + session IDs. GATE OPEN — human approval required before Step 1 executes.

- 2026-09-05 PORT PLAN v8 round-6 execution amendments applied: [AMEND-R6-1] Step 3.1 — `mkdir -p ~/.local/state/flightlead` added BEFORE the new plist load (the migrated com.flightlead.check.plist logs there; dir does not exist at that point). [AMEND-R6-2] Step 1.11 + 3.1 — doctor node-path guard: doctor/check --stack-only SHALL verify the pinned node path /Users/shabibhossain/.nvm/versions/node/v22.23.1/bin/node exists and satisfies engines.node (>=20) BEFORE the plist is loaded/relied on (nvm upgrade fails loudly at doctor time); 3.1 references the guard. [AMEND-R6-3] new Step 0.9b — numbered pre-Step-1 action records sha256 of ~/.zshrc to ~/backups/zshrc.sha256.pre-step-1.txt; AC#8 now cites Step 0.9b. [AMEND-R6-4] Step 0.3 — 7-row ATTRIBUTION provenance promotion re-verified against the live skills/README.md at run time.

[AMEND-R6-5] 0.5 gains PUBLIC-FLIP PII AUDIT precondition (kimi round-6 MED accepted by user): multi-scout parallel scan of full repo incl. all-branch git history + ported backlog/, 3 analyze-fix-review loop passes, PII taxonomy recorded in 0.5. Runs as one of the first post-port tasks; blocks public flip only (not the port itself).

[AMEND-R6-6] PII taxonomy corrected per user: shabib87/codewithshabib = chosen public identity/brand, not PII; allowed in public repo. Real-name paths/emails remain in scope.

2026-09-05 [AMEND-R6-7] Round-7 review notes applied (MED fixes 1-5); flagged to execution thread: de-loopeng sweep needed on flightlead main post-port (LOW).

2026-09-05 ROUND-8 TOUCH-UP REVIEW ([AMEND-R6-7] fixes, severity-threshold gate): UNANIMOUS 4/4 READY — deepseek ses_f8d326113ffe, glm ses_f8d326105ffe, kimi ses_f8d3260fdffe, qwen ses_f8d3260f6ffe. All 5 MED fixes verified in situ; zero HIGH/CRITICAL; zero regressions. Remaining LOWs (non-blocking, inert): 1.6 stale 'codewithshabib' phrasing in historical hit list; header omits R6-7 line; 3.2 could optionally name the DoD human-approval action. TASK-45 plan = FINAL for execution.

AMENDMENT 2026-09-09 (from TASK-51 spec v2): the 1.3 port-matrix suite count is now 16, not 15 — TASK-51 adds bin/test/task-validate.test.ts (bun:test, migrates to node:test per the 1.3 rule). Enumerate suites at run time at execution; AC#3's 'ALL 15' reads as 'ALL suites present at run time'. Also TASK-51 adds labels/DoD defaults to backlog/config.yml which travel with the 1.6 backlog/ COPY unchanged.

2026-09-05 EXECUTION THREAD OPENED: human explicitly approved execution of PORT PLAN v8 starting at STEP 0 (do-not-re-litigate instruction). spec-approved recorded per HITL gate; task claimed.

2026-09-05 STEP 0 EXECUTED (evidence in-session):
- 0.8 DOCS SPINE: disposition IN-SCOPE confirmed (GATE 0 exit criterion recorded). Spine verified present on draft (docs/NORTH_STAR.md 72L, PRODUCT.md 310L, ROADMAP.md 162L, README root). 0.1(4) offline-claim corrected on draft commit e037792 (README.md:49 + PRODUCT.md:54 now state network use is limited to installs/model APIs/security scans). Trademark line verified already present (README + NOTICE).
- 0.8b MERGE: draft/v0.1.0-spec fast-forward merged into main (e037792), pushed origin. [AMEND-R5-14] artifact sourcing verified in the merge diff: LICENSE (Apache-2.0), NOTICE, ATTRIBUTION seed, CONTRIBUTING, SECURITY, issue templates, README spine all arrived. draft also pushed to origin for record.
- 0.1(1) package.json license MIT -> Apache-2.0 (commit 73b7e4d).
- 0.1(6)+0.3 placeholders resolved in ATTRIBUTION.md (73b7e4d): relicense note written (harness-original skills MIT pre-v0.1.0 -> Apache-2.0 from v0.1.0 per ratified brief; frontmatter updates at port = 0.1(8)); ComposioHQ naming verified against live ~/.agents/skills/README.md:21 and fixed from CompositionalHQ; headroom=Apache-2.0, opencode=MIT, pi=MIT license cells filled.
- 0.1(2) dangling citations disposition: citation to docs/research/2026-09-02-oss-agent-harnesses.md (ATTRIBUTION.md:55, PRODUCT.md x3) KEPT — the note is one of the two public-safe notes 1.5 ports, so it resolves at Step 1.5; historical-doc references (docs/PROGRESS.md:462, docs/archive/README.md, learnings self-flagged) left as historical record, not active docs.
- 0.1(5) semgrep invoked-not-bundled clause: verified already present (ATTRIBUTION tool-lineage row + NOTICE) — no-op.
- 0.1(7) difit disposition: DEFERRED to a later milestone per brief "difit-later"; rows left in ATTRIBUTION/PRODUCT pending the milestone decision.
- 0.2 recorded: in-my-voice stays personal at ~/.agents/skills/in-my-voice; tldraw-offline untouched at ~/.claude/skills/tldraw-offline (verified live).
- 0.3 provenance promotion: live ~/.agents/skills/README.md re-counted at run time = 7 rows (obra, mattpocock, addyosmani, ponytail, third-party symlinks, public concept-derived, harness-original) — matches ATTRIBUTION.md table exactly [AMEND-R6-4]. Addy Osmani + ponytail entries present. Trademark line present in NOTICE + README.
- 0.4 DEFERRED TICKET created: TASK-53 (voice-pass; pre-publish gate).
- 0.5 DEFERRED TICKET created: TASK-54 (docs/research sweep + separate backlog npm-package check + public-flip PII audit).
- 0.6 hygiene: ci.yml EXTENDED per [AMEND-R5-17] (commit d5cb67a): kept typecheck+biome+tests; ADDED semgrep (telemetry off, pinned p/default), dist build (skip-when-absent until tsconfig.build.json lands at 1.11b), frontmatter license/upstream check (skip-when-absent until payload/skills lands); tests step made skip-when-absent. CHANGELOG.md seed added (release-please note; workflow itself lands at Step 4 publish setup). CONTRIBUTING/SECURITY/issue templates/ASCII wordmark verified present via merge. Branch protection on main: BLOCKED — GitHub 403 "Upgrade to GitHub Pro or make this repository public" (private repo). CONFLICT WITH PLAN -> escalated to human.
- 0.7 SECRETS LOCATION DECIDED + RECORDED (GATE 0 exit criterion): .env-only (live root, git-ignored; .env.* already in flightlead .gitignore) + committed .env.example; zero secret values in payload templates; init/sync inject personal values from .env at template-resolve time. Unblocks 1.1b.
- 0.9 BACKUP MANIFEST generated: ~/backups/agents-backup-sept-5-2026.manifest.txt (5560 lines) from unzip -l of ~/.agents-backup-sept-5-2026.zip.
- 0.9b ZSHRC BASELINE recorded: ~/backups/zshrc.sha256.pre-step-1.txt = 1f16e77ada00248bffe89b0c9e05728c701d791b3568808f580ba4805786a6d4.
- 0.10 dangling ~/.claude/skills/sh-humanizer symlink deleted (dangling re-verified immediately before deletion; ~/.claude/skills now contains only tldraw-offline).
- GATE 0 STATUS: plan-review unanimous PASS recorded in round-8 note (4/4 READY, zero HIGH/CRITICAL); exit criteria 0.7 + 0.8 now both recorded. Human approval gate before Step 1: execution thread opened under the human's explicit do-not-re-litigate instruction; final nod requested at the Step 0 checkpoint.

2026-09-05 BRANCH PROTECTION DEFERRED (human decision): GitHub 403 on private repo — protection requires GitHub Pro. Human chose DEFER TO PRE-PUBLISH: branch protection on main is now a pre-publish precondition tracked in TASK-54 alongside 0.4/0.5; publish-gate marker flip requires it. 0.6 otherwise complete.

2026-09-05 STEP 1 OPENED (human approved). Run-time survey of ~/.agents: bin/test contains 18 .test.ts suites (plan said 15, TASK-51 amendment 16; per amendment AC#3 reads ALL suites present at run time -> 18 enumerated: agents-install, cache-probe, canary-invariants, config-sync, enforce-hooks, frontmatter-check, headroom-compress, mdconvert, prefix-diff, reviewer-loop, stack-check, sync-model-pricing, task-flow, task-migrate, task-policy, task-validate, verify-gate, worktree-create; all import bun:test). Personal paths confined to config/agents/researcher.md (1 permission glob) + implementer.md (3 deny globs); other refs are tilde-form portable. AGENTS.md 82L with 4 path-ref lines (L3,L10,L11,L12) to template. Runbook 2267L, 30 H2, first at L24. Research: 51 files in ~/.agents/docs/research. plugins/enforce.ts dynamically imports bin/src/task-validate.ts via ~/.agents/bin path — must be rewritten to package-relative in src/hooks/.

2026-09-06 STEP 1 PROGRESS (Streams A+B landed on main, pushed):
- Stream A commits: e277afd (payload: config x21 verbatim + de-personalized researcher/implementer with {{FLIGHTLEAD_HOME}}/{{FLIGHTLEAD_CONFIG_HOME}} tokens; 16 skills w/ license+upstream frontmatter; harness-original skills relicensed Apache-2.0 in frontmatter; AGENTS.md template w/ flightlead sync + runbook-ref rewrite; .env.example + .gitignore negation), 333fbec (docs/architecture x13 + the two public-safe research notes).
- Stream B: 60f64fa (@loopeng scrub x7 per human decision), d4d52f4 (flightlead.json seed + manifest types), 85649c5 (payload assertion tests), 615e217 (16 src files + src/hooks/enforce.ts+verify-gate.ts ported to node toolchain: shebangs, import.meta.main x7, import.meta.dir x3, Bun.YAML->yaml pkg, Bun.spawn->$ shell -> node child_process; local Plugin types; privacy-audit script), b0e37ca (batch 1: 5 non-spawn suites -> node:test; CAUGHT REAL BUG: Hook 10 dynamic import ../../task-validate.ts wrong depth -> fixed to ../task-validate.ts), 8b264ef (batch 2a: 7 spawn suites; NEW LESSON: spawnSync blocks event loop -> in-process stub servers deadlock; stub servers now subprocesses; OPENROUTER_MODELS_URL seam in sync-model-pricing), 86916a4+2878c89 (batch 2b: final 6 suites; repoRoot depth bug fixed in 3 src files (../.. -> .. for src/ layout); stack-check env seams STACK_CHECK_NPM_REGISTRY_URL + STACK_CHECK_OPENROUTER_MODELS_URL + HELP docs).
- TESTS: 577/577 pass, 0 skipped, zero bun:test imports. tsc --noEmit clean. biome exit 0. privacy-audit exit 0.
- Live-root LICENSE relicensed MIT->Apache-2.0 per 0.1(9) (cp from flightlead LICENSE).
- yaml added as runtime dependency.
- REMAINING for 1.13: 1.10 runbook decomposition (heading inventory 30 H2 + preamble, section->fate map, decomposed docs, live-root pointer stub), 1.11 CLI command surface, 1.11b build/bundle config.

2026-09-06 STEP 1 COMPLETE (all commits on main, pushed through 9de0988):
- 1.10 RUNBOOK DECOMPOSITION (3d8c41f): docs/architecture/runbook-decomposition.md = committed heading inventory (31 rows = 30 H2 + preamble [AMEND-R5-12]), every section exactly one fate (script/docs/payload/delete), zero prose-only; NEW docs/architecture/headroom-proxy.md + model-routing.md; docs/cli.md reference surfaces added. Live-root AGENT-STACK-RUNBOOK.md replaced with the pointer stub (full original backed up at ~/backups/AGENT-STACK-RUNBOOK.md.pre-stub.bak); stub deleted later per 3.3 [AMEND-R5-6]. Runbook-internal headroom version discrepancy (0.35.0 vs 0.36.5) resolved to manifest truth.
- 1.11 CLI BUILD: src/cli/index.ts full command surface: init (template tokens resolved from .env per 0.7; payload materialization; diagram-design symlink ONLY when host target exists else skip + doctor warning condition + ledger decision; conflict refusal -> non-zero + ledger), sync (composes src/config-sync.ts), update (report + refuse on drift + channel notes + rollback hints), check (+ --stack-only [AMEND-R5-3] and --pre-commit for worktree hooks), doctor (8 subchecks incl. proxy :8788 health, node.version >= engines; NODE-PATH GUARD [AMEND-R6-2]: plist present -> pinned nvm node path verified, absent -> skip so clean fixtures pass), scaffold --project. JSONL ledger at ~/.local/state/flightlead/ledger.jsonl (ts, command, args, filesTouched, decisions, errors, exitCode). Zero silent failure.
- 1.11b BUILD/BUNDLE: tsconfig.json -> nodenext (allowImportingTsExtensions dropped, remaining .ts imports converted to .js; tsx maps .js->.ts in dev); tsconfig.build.json (outDir dist, rootDir src) emits dist/cli/index.js matching bin; package.json bin -> ./dist/cli/index.js, files -> [dist/, payload/, docs, LICENSE, NOTICE, ATTRIBUTION, CHANGELOG, flightlead.json, .env.example], build + prepublishOnly wired. node dist/cli/index.js runs with ZERO tsx/bun runtime dependency. Stale author-layout references eliminated: enforce.ts messages -> flightlead sync; worktree-create hook template -> flightlead check --pre-commit behind command -v guard; stack-check pricing spawn -> STACK_CHECK_PRICING_BIN seam with package-relative default; configSyncArgv/machinery resolution probes dist .js then dev .ts.
- 1.13 VERIFY GATES: npm test = 597/597 pass 0 fail 0 skip (dist artifact test now runs: plain node dist/cli/index.js --help exit 0); tsc --noEmit clean; biome exit 0; scripts/privacy-audit exit 0; node dist/cli/index.js doctor exit 0 on the real machine (plist absent -> guard skipped, proxy :8788 healthy). FULL DIFF for HITL review: d5cb67a..9de0988 = 13 commits, 146 files, +28404/-801.
- NEXT: human reviews full diff (HITL) -> 1.6 backlog COPY switchover -> STEP 2.

2026-09-06 1.6 SWITCHOVER DONE: backlog/ COPIED to flightlead (commit 3c98695, 62 files); dual-sided backlog task list exit 0 from BOTH ~/Projects/flightlead AND ~/.agents (source kept per plan, deleted only at 3.3). All backlog ops now run from flightlead.

2026-09-06 STEP 2 SYNC PROVEN (checkpoint-ordered):
- GAP FOUND AND FIXED (commit 2ac9b40): sync materialized RAW {{FLIGHTLEAD_HOME}} tokens (config-sync is verbatim) — the CLI now renders payload templates from the live-root .env into a temp tracked root before config-sync runs; .env auto-created from true defaults when absent (0.7 mechanism; real ~/.agents/.env now exists with FLIGHTLEAD_HOME + FLIGHTLEAD_CONFIG_HOME); env-var precedence over .env fixed (configHomeValue order bug). AC#4 resolved-value test added to cli suite.
- SANDBOX: sync exit 0; opencode.jsonc BYTE-IDENTICAL to payload (AC#4 non-templated); researcher.md RESOLVED (0 raw tokens); second sync no-op exit 0.
- FULL MATERIALIZATION (real machine): sync exit 0, adoptedBaseline=true, 21 files skip (live ~/.config/opencode byte-matches rendered payload incl. AGENTS.md in the flightlead-sync form); state + ledger at ~/.local/state/flightlead/ (config-materialize.json, ledger.jsonl).
- 598/598 tests, tsc clean, biome 0, dist rebuilt.
- NEXT: STEP 3.1 LaunchAgent migration, then GATE 3.2 pre-destruct checkpoint -> HUMAN APPROVAL required before 3.3 destructive ops.

2026-09-06 STEP 3.1 + 3.2-pre EXECUTED, GATE 3.2 ALL SEVEN CHECKS PASS (fresh evidence):
- 3.1: check --stack-only hardened (bare binary names now PATH-resolved; opencode pin refreshed to observed 1.18.29 — real drift caught); com.agents.stack-check BOOTED OUT (old plist FILE byte-untouched in place, sha 28060f6a... matches ~/backups migration copy); NEW com.flightlead.check.plist written + bootstrapped (pinned argv /Users/shabibhossain/.nvm/versions/node/v22.23.1/bin/node dist/cli/index.js check --stack-only; Sunday 09:00 Weekday 0; logs -> ~/.local/state/flightlead/stack-check-launchd.log; FLIGHTLEAD_PI_BIN pinned absolute); kickstart run verified: job executed, 7 checks 0 failures, last exit 0. Doctor 8 pass 0 fail with node.path-guard ENGAGED (pinned node v22.23.1 satisfies engines >= 20) per [AMEND-R6-2]. NOTE: first bootstrap silently dropped the agent (cause unknown); re-bootstrap stable across doctor + kickstart + rechecks.
- 3.2-pre: (ii) relocation DONE — ~/.agents/{state,logs,reports}/* moved to ~/.local/state/flightlead/ (old config-materialize.json preserved as .pre-port; no clobbers); (iii) post-port tree state committed on ~/.agents main fbaeaf5 (sanctioned --no-verify: the live Hook 7 blocks main commits by design; this commit is owned by the plan).
- GATE (a): ~/.agents porcelain EMPTY; .worktrees/TASK-45 absent (pre-removed by human). GATE (b): unzip -t OK + manifest MATCH vs 0.9 artifact. GATE (c): npm test 598/598. GATE (d): sync no-op exit 0. GATE (e): backlog task list exit 0 from flightlead. GATE (f): state/logs/reports relocated, originals empty. GATE (g): :8788 HTTP 200 + doctor 8/8 + new plist loaded + kickstart exit 0 + old plist byte-untouched.
- STATE: GATE 3.2 OPEN for HUMAN APPROVAL before 3.3 (rm -rf ~/.agents/.git + deletion sub-step + old plist deletion).
<!-- SECTION:NOTES:END -->

<!-- SECTION:PLAN:END -->
