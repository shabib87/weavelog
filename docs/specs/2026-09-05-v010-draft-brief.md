# v0.1.0 Draft Brief (conductor record)

Ratified 2026-09-05 via wayfinder map TASK-45 + grilling. This file is the single
brief for drafting docs/specs on `draft/v0.1.0-spec`. Reviewers audit against this.

## Product identity

- Name: weavelog. Tagline: "The inner harness: agent work you can audit."
- License: Apache-2.0 + NOTICE; ATTRIBUTION.md for lineage; name/logo © Shabib Hossain (not code-licensed).
- v0.1.0 = opencode stranger test: `init` writes loadable opencode config, skills to
  scanned path, ≥1 enforced gate, file manifest, `doctor` (installed? authenticated?
  config parses? proxy up? versions pinned? arm64?). Platform: arm64 macOS only.
- Milestones: 0.1 opencode → 0.2 pi+plugins → 0.3 claude code (subscription-mode:
  no cross-model OpenRouter reviewers; tiering degrades to single-provider) → 0.4 codex
  → 1.0 (codex shipped). npm publish at 0.1.0; brew tap at 0.2+.
- Distribution: npm (OIDC trusted publishing), tag-driven release-please over
  conventional commits; CHANGELOG.md automatic; CI gates publish on privacy-audit-done.

## CLI surface

| Command | Job |
|---|---|
| `weavelog init` | install opinionated deps (headroom[proxy], opencode, markitdown, semgrep, backlog.md, difit-later), materialize ~/.agents/* + ~/.config/opencode/* (two-step user-modification flow), never silently overwrite managed files |
| `weavelog sync` | dev path: repo → live (dogfood loop; ported config-sync.ts) |
| `weavelog update` | release-driven dep bump + re-materialize |
| `weavelog check` | deterministic gates: tests, lint, typecheck, semgrep (telemetry off, pinned rulesets), secrets, frontmatter, manifest completeness |
| `weavelog doctor` | installed? authenticated? config parses? proxy healthy? cache mode=cache? python3.13? :8788 launchd-owned? semgrep smoke? ledger tail? |
| `weavelog scaffold --project` | project: backlog init, AGENTS.md, docs/research, ADRs, .gitignore, .env.example (never touches .env/.env.local) |

- Manifest: JSON (`weavelog.json`), CLI-managed; per-tool install channel (brew/pipx/npm/uv)
  + version + doctor check id. Completeness rule: every external binary invoked in skills/src/payload
  must appear in manifest (check-enforced).
- Ledger: append-only JSONL at ~/.local/state/weavelog/; every command logs files touched,
  decisions, errors, exit code. Zero silent failure: refusal = log line + non-zero exit.
- Post-flip ~/.agents: ONLY open-standard files (AGENTS.md, .agents/skills/, host-standard dirs).
  No git, no node_modules, no state. Sync state → ~/.local/state/weavelog/.
- Host-specific plugin files are CLI-managed artifacts (src/hooks/, emitted per host).
- ~/.agents/docs → weavelog/docs (privacy sweep gates public flip). Personal second-brain
  notes stay local unversioned. Backlog moves to weavelog/backlog now.

## Port matrix (from ~/.agents)

| Asset | Disposition |
|---|---|
| config/ (agents, harnesses, prompts, opencode.jsonc, opencode-AGENTS.md) | PORT → payload/ (de-personalized templates; per-host generation) |
| skills/ (as-* + originals + symlink policy) | PORT → payload/skills/ (frontmatter license/upstream kept) |
| bin/src/*.ts (config-sync, task-flow, worktree-create, stack-check, frontmatter-check, agents-install, reviewer-loop, sync-model-pricing, mdconvert, prefix-diff, cache-probe, headroom-compress) | PORT → src/ (centralized; plugins/ → src/hooks/) |
| docs/architecture/ | PORT → docs/architecture/ (updated to post-weavelog model) |
| docs/research/ | PORT after privacy sweep |
| backlog/ | MOVE to weavelog/backlog |
| AGENTS.md constitution | PORT → payload template |
| .pi/, .pi-subagents/, .headroom/ | STAY (instance); pattern seeds milestone 0.2 |
| state/, .worktrees/, backups | STAY (instance/machine) |
| stack-versions.json | BECOMES weavelog.json stack manifest |
| AGENT-STACK-RUNBOOK.md | DECOMPOSED (see below) |

## Runbook decomposition rule

Every section gets exactly one fate: script (init/check/doctor/update logic),
architecture doc (docs/architecture/<topic>.md), payload template, or delete.
Nothing stays prose-only. Key mappings: fresh-Mac checklist+Phase 0 → init;
Phase 1 health/smoke/ownership/cache-mode → doctor checks + docs/architecture/headroom-proxy.md
(cache-mode rationale, pricing-sync why, :8788 single-owner); Phase 2 → payload/opencode.jsonc
template + doctor config-parse; Phase 3 → payload/agents templates; Phase 4 gotchas → init
symlink logic; pricing sync → src/sync-model-pricing.ts + doctor; model tiers → payload
models.json default + docs/architecture/model-routing.md (protocol stays, dated table becomes
example); safe-update procedures → update; verification battery → doctor subchecks; prune
rules → backlog tasks with triggers; carry-over manifest → this port; quick reference → README
quickstart + docs/cli.md.

## Product docs spine

- NORTH_STAR: rewrite — hosts = weavelog-composed (opencode first, pi 0.2, claude 0.3,
  codex 0.4); human directs AND verifies (conductor, one-question dialogue, plan+merge gates);
  composition over invention; evidence over claims (receipts, Run Records, dated corpus);
  open standards (AGENTS.md, Agent Skills); local-first, terminal-native, arm64 macOS v1;
  open-weights primary, frontier escalation, subscription-mode degradation noted for 0.3/0.4.
- PRODUCT: keep 3-layer model, PMF/dogfooding, credibility, quality bar, direction control.
  Replace moat (moat v2: audit layer + headroom + compounding corpus; wedge = scaffold) and
  landscape (one-punch, keel, outrigger, tiller-ai, conductor, AO — Sep 2026 scan). README
  carries the 4-section spine: what/is-not/principles/moat + who-for + honest status.
- ROADMAP: milestone ladder with per-rung deliverables (stranger test per host).

## Hygiene set (now)

LICENSE (Apache-2.0), NOTICE, CONTRIBUTING.md (issues welcome; PRs not accepted v1),
SECURITY.md, CHANGELOG.md (release-please), ATTRIBUTION.md (promote skills/README table +
tool lineage + inspiration section; semgrep = invoked-not-bundled LGPL note), issue templates,
CI (typecheck, biome, tests, semgrep telemetry-off, frontmatter check), .gitignore (.env,
.env.local from commit 1), branch protection main, ASCII wordmark logo, trademark line
("weavelog name/logo © Shabib Hossain — not covered by the code license").

## Fog (tracked, not built)

Second-brain standardized dir in .agents; LaunchAgent/scheduled checks; subscription-mode
model tiering (0.3/0.4); reviewer roster degradation to single-provider; brew formula;
real logo; docs site; governance/PR acceptance.
