# Research archive

Dated, source-attributed research notes. READ THESE BEFORE re-researching anything covered —
re-derivation protocols are included where live data goes stale.

Convention: `YYYY-MM-DD-<topic>.md` with YAML frontmatter (date, topic, status, sources,
models_used_for_research, supersedes). New research that updates an old note sets `supersedes`
and the old note's frontmatter `status: superseded`.

## Index (2026-08-15)

- openweight-model-tiers — model lineup, pricing, 1M-ctx rule, quarterly re-derivation protocol
- openrouter-prompt-caching — caching mechanics, sticky routing, setCacheKey, probe evidence
- headroom-pricing-internals — litellm resolution, alias map, the three-part $0.00 fix, upgrade hazard
- headroom-savings-diagnosis — WHY dashboard shows ~5% (compression by design + cache attribution gap)
- skill-pack-selection — superpowers vs agent-skills decision
- agentic-test-guardrails — TDD/BDD standards, responsibility split, anti-patterns
- addy-loop-engineering-series — synthesis of Addy Osmani's 4 Aug 2026 posts (loops, quality, outer loop, factories)
- loop-primitives-claude-codex-sdk — verified /goal, /loop, /schedule, workflows, agent-loop from live docs
- inner-harness-vocabulary — reviewer-corrected terms for the loop engineering helper
- inner-harness-layers — the two-axis tooling layer definitions (capability/control/packaging/infrastructure)
- host-state-verification — live probes of headroom, opencode, pi, the proxy-ownership race
- unified-sequencing — the 3-reviewer-approved sequence merging the installer plan + the headroom-fix plan (two tracks: build-time + install-time)
- loopeng-helper-plan — the implementation plan (phased, dogfooded, worktree-chunked)
- opencode-cache-root-cause — why opencode sessions show ~5% cache rate; cache key vs session pinning, setCacheKey, proxy layer attribution
- headroom-sdk-utilization — headroom SDK + proxy usage tracking; cost/usage measurement via the proxy

## Index (2026-08-16)

- agentsmd-hygiene — deterministic standards for AGENTS.md (LOC ceiling, single-source, command-first, every-rule-paired-with-enforcement); AgentLint + pre-commit + custom `agentsmd-check.ts` stack; 6 machine-checkable properties
- agentsmd-root-vs-subdirs — opencode unions AGENTS.md (no conflict resolution); global=procedure, root=facts, leaf=declarative; one canonical file + symlink adapter, never duplicate
- primitive-selection — decision tree across prompt/skill/agent/script/workflow/hook; user's biggest gap is ZERO hooks; no-closing-ceremony=AGENTS.md, verify-with-criteria=skill+hook pair
- model-tiered-agents — ladder flash→glm→qwen→kimi mostly optimal; sharpen "2 attempts" to "2 different attempts"; add StepBudget/CostBudget; don't escalate context-overflow (trim instead); minimax-m3 for routine vision not kimi
- terminal-markdown-viewer — glow vs frogmouth rubric: adopt glow v3.0.0 (4.80 vs 1.25); frogmouth abandoned (no push since 2024-08) + TUI-fit veto; exit trigger = no commit >2mo
- work-management-layer-selection — ADOPT backlog.md (tracker+kanban/web viz, zero harness conflict); GSD identity resolved: gsd-core=command pack (3rd orchestrator → YAGNI), gsd-pi=standalone agent (replaces opencode → reject); beads/beans rejected w/ named triggers
- (see plans/2026-08-16-light-factory-plan.md) — factory model, three phases × two loop roles (phases: WHY/WHAT/HOW; loop roles: inner=agent execution, outer=human ownership; supersedes the plan's original "two-loop" phrasing — see TASK-22); superpowers removed, 4 keepers ported; 6-agent roster (researcher+implementer added); backlog.md queue; verify-gate hook; task-flow.ts
- addy-osmani-deterministic-agentic-workflows — deterministic agentic workflow theory (deterministic scaffolding vs LLM judgment, the SDLC loop, orchestration patterns)
- agent-cli-portability — opencode vs claude code vs codex vs pi.dev vs hermes; the minimal inner-harness contract; emerging standards (AGENTS.md, MCP, SKILL.md)
- opencode-capabilities — hooks, workflows, subagents, plugins, permissions, config, skills
- backlog-md-state-model — backlog.md v1.50.1 has NO phase/step concept; flat status enum (configurable, default To Do/In Progress/Done, local adds In Review, MCP adds Draft); no transition enforcement; only hook = post-status `onStatusChange` (no pre-gating); issue #456 lifecycle hooks unshipped; stable JSON read API (`--json`, schemaVersion 1); recommendation: SDLC phase state lives OUTSIDE backlog in a separate per-worktree state file, harness reads backlog via CLI --json + onStatusChange trigger, gates via opencode plugin hooks
- headroom-mcp-removal — decision + rationale for removing the headroom MCP server from opencode config (proxy is CLI/plugin territory, not MCP)

## Index (2026-08-19)

- agentic-worktree-harness-tools — verification of 4 tools cited in the worktree-harness plan: backlog.md (as described; --json + deps + parentTaskId confirmed; custom frontmatter round-trip OPEN), difit (as described; real npm CLI, difit-review skill exists, --comment preload JSON contract documented), PR-Agent (partially as described; self-hostable + local-LLM via LiteLLM/Ollama confirmed, BUT local-diff-without-PR needs new plain-diff mode v0.40.0 --stdin/--diff-file, default CLI takes --pr_url; PyPI stale at 0.39.0), Archon (as described; real harness builder w/ DAG depends_on + worktree isolation; issue #957 is the rewrite announcement, not a design RFC); cross-cutting: backlog.md=WHAT queue, Archon=HOW runner (no bridge shipped), difit+PR-Agent need glue to reshape review output into --comment JSON
- opencode-headless-api-harness-binding — opencode has BOTH a CLI (`opencode run --format json --dir <worktree> --auto "prompt"`) AND a TS SDK (`@opencode-ai/sdk`, in-process via createOpencode or serve+HTTP); SDK gives session.abort, SSE events, structured output (json_schema + retryCount), plugin hooks (tool.execute.before/after, custom tools, shell.env) — but NO timeout/max-turns/budget flags and undocumented exit codes; Archon uses @opencode-ai/sdk as managed embedded runtime (spawns+owns server, random pw, tears down); verdict: hybrid — define portable IAgentProvider contract, implement opencode-native via SDK (budget/stuck/done-check all need in-process hooks), keep `run` CLI as fallback; Archon itself enforces structure at DAG layer not inside agent

## Index (2026-08-23)

- architecture-pattern-extraction — cross-referenced extraction of net-new value from two architecture-pattern docs vs existing harness; 5 extracts ranked (add deepseek-v4-pro-0813 as 2nd reviewer, sequential review chain, reasoning-depth escalation, 4-layer payload naming, conductor-boundary health check); ZDR header hallucinated; reasoning_effort placement wrong; Flash-as-conductor and Kimi-as-researcher rejected as cost regressions
- backlog-md-capability-audit — full v1.50.1 surface (CLI+MCP+config) vs harness usage; 10 underutilized capabilities ranked; top gaps: onStatusChange callback, backlog decision (merge-gate records), backlog document, --modified-files traceability, --implementation-notes/--comments/--final-summary, --check-ac at gate, DoD checklist, milestone for DAG phases; no custom frontmatter mechanism (labels only); MCP is CLI subset (no decisions/board/drafts/cleanup/config); CLI is recommended integration path
- difit-opencode-sdk-integration — verifies harness integrates difit (CLI-only: --comment, --background, difit-review skill) and opencode (SDK: createOpencode, session.prompt/abort, event.subscribe, noReply for context injection, StructuredOutputError handling; plugins: tool.execute.before/after, session.idle/diff/error, permission.asked/replied, experimental.session.compacting, custom tool(), shell.env; agent steps limit, --auto, permission globs) per official docs; cross-cutting verdict: MCP for backlog.md, SDK+plugins for opencode, CLI for difit — all match docs; --background JSON includes pid (kill handle), auto-adds --keep-alive + --no-open
- headroom-openrouter-tool-research — Headroom (v0.35.0 compression proxy on port 8788, --mode cache --backend openrouter) + OpenRouter (pure HTTP API, full param list, reasoning_effort enum where max=xhigh per docs, zdr, caching/sticky routing cost model); call chain opencode→headroom:8788→OpenRouter; flags: reasoning_effort passes through (not stripped), headroom overwrites session_id (sticky routing impact), CCR TTL 1800s for long runs, MCP removal still pending live edit, cost accounting post-compression
- agentic-architecture-pattern — conductor-mediated agentic architecture, compound design pattern matrix + light factory engine (consolidated 2026-08-30 from the duplicate `agentic-deisgn-pattern`, which is superseded)

## Index (2026-08-24)

- opencode-attachments-subagent-passthrough — context7-verified: opencode `attachment.image.{auto_resize,max_width,max_height,max_base64_bytes}` governs RESIZE/REJECT only (no passthrough); agent schema (`agent.ts` Info class) has NO attachment-routing/modality-override/passthrough field; modality check tied to active model's declared `input_modalities` (models-dev.ts literals text/audio/image/video/pdf); `enabled_providers` is a global provider allowlist (v2 → experimental.policies); NO `vision_model`/`attachment_model`/`small_model`(v2 removes) concept; realistic fixes: vision-capable conductor, or reference image by file path so subagent's read tool loads it, or upstream feature request

## Index (2026-08-25)

- headroom-openai-include-usage-bug-report-prep — pre-flight for filing a bug to headroomlabs-ai/headroom: no duplicate issue found (closest #2957 is Responses API WS fallback, distinct from Chat Completions stream_openai_message include_usage injection); CONTRIBUTING.md routes bugs to PRs not issues (issue creation restricted); PR requires repro + failing test + "Real behavior proof" section or autoclosed; no CLA; Contributor Covenant v2.1 CoC; recommended format is PR with fix titled `fix(proxy): inject stream_options.include_usage in OpenAI Chat Completions streaming path`

## Index (2026-08-28)

- diagramming-agent-skill — diagram-design Agent Skill install for opencode (TASK-14): skill hub placement, symlink chain, verification
- inner-harness-proxy-plugin-design — pi + opencode routed through the local headroom proxy (Layer A routing + Layer B toolResult compression via pi extension); cache mode non-negotiable; include_usage bug + local hack + upstream status; deprecated @ryan_nookpi extension removal; measured thresholds (minContextTokens=100000, minMessageChars=10000); safety contract (never spawns/configures the proxy, only reads /health + /stats + calls stateless /v1/compress); .zshrc cleanup. §10 post-hoc corrections (2026-08-30): mode is at `/stats summary.mode`, thresholds at `~/.pi/agent/headroom/settings.json`

## Index (2026-08-29)

- spec-driven-development-in-ai-harnesses — Gherkin/BDD ceremony debate (U-shaped cost curve, EARS vs Gherkin); 1:many spec→task mapping (all SDD tools converge on Specify→Plan→Tasks→Implement); deterministic verification = test exit codes not LLM judgment; KISS recommendation: the backlog task IS the spec (description + ACs), ACs are EARS-inspired WHEN/THEN with embedded verification commands, gate = `bun test` exit code, no Gherkin/Cucumber/Spec Kit; 10 YAGNI candidates (don't build separate spec.md/plan.md/tests.md/results.md/verdict.md per task — they duplicate backlog.md fields)
- backlog-mcp-enforcement-bypass — (filing gap: this note exists but was not indexed)
- lsp-vs-cli-diagnostics-in-opencode — LSP vs CLI diagnostics in AI coding agents (opencode); tsserver 200MB-3GB/instance (microsoft/TypeScript#46028,#18055; vscode#140090), bash-ls+ShellCheck CPU lockup (#736), yaml-ls Node-class; opencode #5259 unbounded LSP output→token quota (fixed #5480), #3297 agents ignore LSP + silent startup failures (#1939), claude-code#64239 staleness; counter: OpenDev arxiv 2603.05344v1 bounded design (3s debounce, Error-only, ≤20 diagnostics, 2-level cache, lazy+crash-restart); tsserver diagnostics ≈ `tsc --noEmit` (redundant for diagnostics, LSP win = symbol nav); verdict: CLI-first default, enable LSP selectively+bounded only where CLI diag is slow/absent; confidence medium-high; gaps: no hard RAM for bash-ls/yaml-ls, no controlled LSP-on/off codegen benchmark

## Index (2026-09-02)

- oss-agent-harnesses — live-fetched comparison of OSS agent harnesses vs loopeng: outrigger (evidence lab; blind merge gate DEMOTED by its own 5.9x-cost experiment; held-out examiner + run-ledger/shadow-pilot = loopeng's main borrowable), one-punch (outrigger successor; closest sibling — same mattpocock-skills DNA, one-decision-at-a-time, contracts compiled not interviewed), keel (out-of-process warden + hash-pinned policy + tamper-evident audit + Seatbelt/bubblewrap; pre-alpha npm 0.1.2), Trammel (stdlib-only beam-search planner + recipe memory + step-claiming, MCP/PyPI, stale since 2026-07), tiller-ai (npx scaffolder for Claude Code/Copilot CLI/OpenCode, conventions+hooks only, dormant since 2026-03), baton (multi-machine collab server, orthogonal), ramannanda9/agent-harness (BYO-LLM framework: BudgetGuard/HITL plan-mode/checkpoint-resume — cheapest adoptable runtime plumbing), landscape: microsoft/conductor 415★ (deterministic YAML routing + human gates), AO/agent-orchestrator 10.9k★ (desktop, 26 agents, worktrees), SupaConductor 375★ (AGPL, dormant), llm-conductor (markdown-only dev/review/judge incl. opencode); verdict: loopeng AHEAD on process (WHAT/HOW, dialogue, research layer) + unique proxy stack, BEHIND on held-out suites, hard budget caps, packaged distribution, fleet observability; collision check: batuta COLLIDES (paiml/batuta + @vorluno/batuta-mcp), wayline COLLIDES (bare npm taken + K8s-DAG repo), caesura partial (@caesura-io org active in AI), fermata/downbeat/sides clear
- loopeng-moat-analysis — moat/defensibility analysis for the OSS launch: (a)-(e) asset verdicts ((c) headroom = strongest technical moat, contested by TokenShift; (b) evidence corpus = #2; (a)(d)(e) = copyable hygiene); opencode platform risk (203k★/MIT, V1↔V2 compat, absorption risk via CF-AI-Gateway passthrough + compaction hooks); compression prior-art map (LLMLingua/Token Company/gateways/native caching); auditable-agent-process demand (NIST CAISI 2026-02, EU AI Act Art 26(6) 6-mo retention, high-risk deadline 2027-12-02 post-Omnibus); 3 ranked positioning options; TOP 3 pre-launch actions + what NOT to build

## Index (2026-09-03)

- agent-skills-ponytail-adoption — final skill/agent evaluation set: addyosmani/agent-skills 25-skill disposition table (3 distills, 3 meta-steals, 19 rejects — router explicitly rejected) + DietrichGebert/ponytail steal verdicts (adopt ladder + canary-invariants; defer promptfoo eval harness → TASK-42; reject always-on plugin + E–J) + attribution policy (MIT, SHA-pinned headers for technique steals) + watchlists (wayfinder/prototype 90-day trigger, general-agent open decision)
- heldout-suite-macos-seatbelt — held-out acceptance suite for loopeng: outrigger heldout-suite mechanics verified live (materialize-outside-repo refusal, fails-on-base ≥1 in clean checkout, seal→sha256 manifest anchor in git ledger, verify-before-every-use, deny-fragment.json for spawner merge, merge-gate 100-line output tail = counts-only-adjacent); macOS Seatbelt 2026 feasibility (sandbox-exec deprecated-since-10.8 but functional on Sequoia, used by Bazel/Nix/Claude Code/Codex, no replacement per containerization#737; `(deny file-read* (subpath …))` repro'd in sandbox-runtime#8, rename bypass fixed with file-write-unlink-on-ancestors, CVE-2026-20628 patched macOS 26.3); verdict: v0 = heldout.py + authoring contract + counts-only gate + opencode permission deny-globs (policy layer, S, no hooks), v1 = Seatbelt wrapper + verify-gate hook (M); highest-leverage item = materialize-outside-repo + seal/verify manifest (evidence-first, sandbox-free on day one)

- budget-caps-checkpoint-resume — deep-dive for loopeng OSS budget/resume gap; keel budget env table (KEEL_MAX_TOKENS = cost-true, cached-input-discounted primary ceiling; KEEL_MAX_GROSS_TOKENS raw backstop + compaction runway trigger; KEEL_MAX_WALL_SEC graceful `deadline` stop; KEEL_MAX_TURNS not a spend cap; budget/deadline = distinct non-zero exit outcomes; append-only JSONL session ledgers + --continue/--resume/`sessions branch`), ramannanda9/agent-harness (BudgetGuard auto-wired per distinct LLM instance, per-call-site cost attribution via classifier/router/planner/synthesizer slots; CheckpointStore file+Redis auto-resume in dispatch_stream; RunState serializable step+phase+per-action status+budget+failure; at-least-once crash-resume with ActionState.attempts; JSONL trace recorder+replay), opencode native (VERIFIED in types.gen.ts: AssistantMessage.cost + tokens{input,output,reasoning,cache.read,cache.write} — cache reported separately so cost-true discounting computable natively; message.updated events carry full Message; NO budget/spend param anywhere in server/SDK API tables; enforcement surface = plugin event hook accumulate + tool.execute.before throw + client.session.abort; persistence = /session/:id/fork at messageID + revert/unrevert + summarize + session.messages replay; build order: opencode budget-guard plugin v0 = highest-leverage single item (S effort, native primitives only); gaps not checked: keel discount math source, BudgetGuard hard-stop semantics, cost field provenance)

- loopeng-packaged-distribution — distribution mechanics from 4 live-fetched peers: tiller-ai `npx init` (npm bin + commander/@clack prompts; per-tool adapter trees .claude/.github+.vscode/mcp.json; .tiller manifest records managedFiles → `upgrade`), keel (`private:true` root pkg → separately built `keel-harness` npm carrier via `pnpm package`; `keel doctor` = quickstart step 2 — sandbox/credential checks, one-copy-paste-fix, source-tree self-awareness), conductor (curl installer + uv pinning + SHA-256 + plugin marketplace), AO (GH Releases latest/download permalinks + auto-update); license verdict Apache-2.0 (patent grant; MIT only for min-friction); v0 = fresh repo + secrets audit + de-personalized tree + install.sh + doctor + CI [M]; v1 = npx init scaffolder + manifest upgrade + OIDC publish [L]; build order ends in doctor→install→tag; HIGHEST-LEVERAGE = `loopeng doctor` (stranger acceptance test = CI gate = de-personalization driver = v1 init core). Feeds TASK-34/28/29/30.

## Index (2026-09-05)

- agent-harness-host-support — what "supported <host>" requires for a scaffolding harness CLI; two adapter meanings (launcher vs scaffolder) from tiller-ai (shared .tiller core + per-host trees; OpenCode=scaffolded-not-supported), AO (26 harnesses = readiness-probe+spawn contract, Chat drivers only for codex/claude-code/opencode/droid), looprail (process adapters; cursor NOT in its README list despite dispatch claim); per-host table for opencode/pi/Claude Code/Codex (config file, skills path, hooks surface, subagent defs, context file, trust gate); shared core = SKILL.md dirs + context md + doctor probe; bespoke = config format, hooks wiring, agent frontmatter; effort opencode=M, pi=S/M, claude=M, codex=L (unverified subagent format); stranger-test definition of "supported" (config loads + skills scanned + one gate + manifest + doctor + pinned version); sequencing opencode→pi→claude→codex rationale; gaps: pi models.json filename, Codex subagent format, Claude Code hooks schema re-verified only via tiller

## Index (2026-09-06)

- sandboxing-substrates — local execution-sandboxing for weavelog on macOS ARM: Colima (MIT, 30.7k★ active; `--vm-type vz --mount-type virtiofs`, socket ~/.colima/default/docker.sock per FAQ) recommended substrate; zero-dep Bun `fetch(…,{unix})` over Docker Engine API (containers/create → exec create/start hijacked stream + stdcopy demux → GET /exec/{id}/json ExitCode → DELETE force) recommended orchestration; ephemeral-container-per-task + destroy-on-finally rollback with worktree bind-mount (worktrees=what changes, container=where it runs); OrbStack (proprietary $8/user/mo commercial) + Docker Desktop rejected on OSS/licensing; Daytona rejected (no root LICENSE found via API, pushed 2026-07-24 stale); E2B cloud-first/Terraform+Nomad = reference only; Docker sbx (NOASSERTION binaries) + apple/container (Apache-2.0 VZ-per-container, v1.3.1) = watchlist; ArcBox 404/unverifiable; OpenHands patterns adopted (hash-tagged image layering, overlay-COW bind mounts); confidence medium-high substrate/orchestration, medium state-machine; 7 POC unknowns (top risk: linked-worktree .git file breaks git under single-dir bind-mount). FILED OUTSIDE weavelog repo — mirror to repo docs/research/ required (repo writes denied for researcher)
- cli-bundling-vs-composition — headroom PR #2677 case study (rtk/lean-ctx removed 2026-07-31: unverified binary supply chain, durable-state orphans → 400-line purge module, #2344 gate rot masked by e2e exporting HEADROOM_RTK=1, wrong-arch #2700); rule = compose by reference never by ownership (check/pin/wire with provenance-fenced reversible blocks, never fetch/vendor/manage — retired flags must fail loudly not silently no-op); tier model required / recommended+checked (semgrep, rtk) / personal satellites (tldraw) with loud degradation; open risk = wiring is trust escalation (composed tools execute every session; weavelog's own npm distribution shares the exposure class: transitive deps, typosquatting, xz/event-stream-style, maintainer compromise); FILED 2026-09-06 as TASK-62 (pipx→uv + repo channel vocab), TASK-63 (npm+CI supply-chain hardening), TASK-64 (nvm→brew node@22, Kimi tie-break 3/3: the launchd pi pin was half-illusory — env-node resolves to floating brew v25), all m-7 deps TASK-59, publish-gate closure set {53,54,56,62,63,64}, plus deferred TASK-65 (composed-surface trust model L0-L4, revive trigger recorded); rejected precedent = headroom-desktop pinned opt-in bundle (revisit only for turnkey installer); local probes: headroom 0.36.5 tools.json has no rtk, rtk 0.38→0.48 changed grep flag semantics (drift argument), include_usage backend bug still unfixed on upstream main (live-fetched). FILED OUTSIDE weavelog repo — mirror to repo docs/research/ required.

## Index (2026-09-07)

- flagship-tier-pricing-quality-preliminary — TASK-61 spike: 7-candidate OpenRouter pricing/quality matrix for the ADR-004 roster (deepseek-v4-pro-0813, glm-5.3, qwen3.8-2.4t, kimi-k3 pro tier; flash tier flat-pricing cache reality); seat-weighted benchmark composite inputs; DeepSeek first-party peak hours (ET) vs OpenRouter flat pricing; GLM-5.3-flash promo expiry 2026-09-09; fed ADR-004 + TASK-75 roster adoption
- model-redistribution-cost-quality (2026-08-30) — 6-model roster cost/quality rebalancing before ADR-004
- openrouter-pricing-litellm-headroom-hooks (2026-09-05) — pricing bridge scope for sync-model-pricing.ts
- headroom-memory-opencode-bounce (2026-09-06) — headroom proxy memory-tool injection fix + opencode bounce
